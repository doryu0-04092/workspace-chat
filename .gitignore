# **行を足したら、scripts/doc-scope.sh の一覧にも振り分ける。**
#   ファイル名（スラッシュ無し） … DOC_PRUNE_FILES（値を持つ）か DOC_NO_VALUE_IGNORES（持たない）
#   ディレクトリ（末尾が /）     … DOC_PRUNE_DIRS
#   打ち消し（!）                … DOC_KEEP_FILES。**ただし DOC_PRUNE_DIRS の配下を指すものは足さない**
#                                  （doc_excluded がディレクトリ側で先に返るため、KEEP は届かない）
#
# **裸の名前（`/` も glob も含まない行）は、gitignore(5) ではファイルにもディレクトリにも一致する。**
#   ディレクトリのつもりなら **末尾に `/` を付ける。** 付けないと DOC_PRUNE_DIRS へ届かず、
#   **その配下の Markdown が検査の一時ディレクトリに複製される**
#   （0c-3 は、そのディレクトリが**実在するときだけ**落とせる。**綴りからは判定できない**）
#
# **スラッシュを含む行は、先頭のディレクトリ名で決まる。**
#   先頭が DOC_PRUNE_DIRS にある（例: `.vscode/*`）… **そのままでよい。** 0c-3 は読み飛ばす
#   先頭が DOC_PRUNE_DIRS に無い（例: `infra/*.tfvars`）… **DOC_PRUNE_FILES に足しても効かない。**
#     DOC_PRUNE_FILES / DOC_KEEP_FILES は **basename にしか当たらない**（find -name / case "$base"）。
#     足しても永久に一致しないのに 0c-3 は緑になる（**偽の緑**）。
#     **先頭のディレクトリ名を DOC_PRUNE_DIRS へ足す。**
#
# **`.vscode/*` を `.vscode/` に書き換えてはならない。**
# 直下の `!.vscode/extensions.json` が**永久に効かなくなる**——
# git は除外されたディレクトリの中を列挙しないため、**親が除外されたファイルは `!` で戻せない**
# （gitignore(5) の明記）。`*` 付きで書いてあるのは、その制約を避けるためである。
# **この壊し方は、第5巡まで 0c-3 が1つも捕まえなかった**（3つの読み飛ばしにすべて当たるため）。
# いまは `doc_unreachable_keeps` が落とす。
#
# **振り分けないと check-docs.test.sh の 0c-3 が落ちる**（#44）。
# 落ちる側にしてあるのは、**振り分けを忘れると、値を持つファイルが
# 検査の一時ディレクトリに複製されて残り続ける**ためである。

# 依存
node_modules/
.pnp/
.pnp.js

# ビルド成果物
dist/
build/
.vite/
*.tsbuildinfo

# 環境変数（値は絶対にコミットしない）
.env
.env.*
!.env.example

# ログ
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# テスト・カバレッジ
coverage/
.nyc_output/
playwright-report/
test-results/
blob-report/

# Prisma（生成物）
**/generated/

# Terraform
# .terraform.lock.hcl は意図的に無視しない。プロバイダのバージョンとチェックサムを
# 固定するファイルであり、追跡しないと手元・CI・本番で違うプロバイダが入りうる。
# 代償: 記録した platform のハッシュしか検証に通らない。手元と CI の OS が違うと
# terraform init が落ちる。ロックは複数 platform を指定して作る
# (terraform providers lock -platform=darwin_arm64 -platform=linux_amd64)。
.terraform/
*.tfstate
*.tfstate.*
# *.tfvars は末尾一致のため prod.tfvars.example には一致しない。
# .env と違い、例示ファイルを打ち消す行（!*.tfvars.example）は要らない。
*.tfvars
# Terraform は JSON 版の変数ファイルも読む（terraform.tfvars.json / *.auto.tfvars.json）。
# 上の *.tfvars は末尾一致のため、これらには一致しない。書かないと
# secrets.auto.tfvars.json に置いたパスワードが無視されずコミットされうる。
*.tfvars.json
crash.log
override.tf
override.tf.json

# IDE / エディタ
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# エージェントが作る git のワークツリー。apps/ の複製がまるごと入る。
# eslint.config.js と .prettierignore は既にここを除外している（PR #33）。
# 追跡していないだけでは無視はされない。ワークツリーは中に .git を持つため、
# `git add .claude` を打つと、複製された apps/ 一式ではなく
# **埋め込みリポジトリ（gitlink）1エントリとして誤って登録されうる**
# （実測: `git add --dry-run .claude` で
# `warning: adding embedded git repository: .claude/worktrees/agent-...` が出た）
# （#35）。
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# アップロードの一時領域（ローカル開発用）
uploads/
tmp/

# テストレポート（CI では Artifact として残す）
reports/

# npm audit --json の出力。CI が作る中間ファイルであり、追跡しない。
audit-report.json
