# プロジェクト運用ルール

## プロジェクト概要

Slack 風のチャットアプリケーション。スクール課題。

ワークスペース単位でチャンネル（パブリック / プライベート）を持ち、スレッド・リアクション・
メンション・検索を備えたリアルタイムチャットを提供する。認証必須で、非ログインでの閲覧はしない。

- [要件定義書](docs/requirements.md)
- [機能一覧](docs/features.md)
- [技術スタック](docs/tech-stack.md)

## 技術スタック

- フロントエンド: React 19.2 + Vite 7 + TypeScript 5 + Tailwind CSS 4
- バックエンド: Node.js 24 LTS + NestJS 11 + Socket.IO 4 + Prisma
- DB: PostgreSQL 17 + pg_bigm（ローカルは Docker Compose）
- インフラ: AWS（CloudFront / S3 / ALB / ECS Fargate / RDS / ElastiCache）+ Terraform

バージョンの選定理由と LTS の根拠は [技術スタック](docs/tech-stack.md) に記す。
**バージョンを変える場合は、必ずその文書も更新する。**

---

## このプロジェクト固有のルール

### 1. 要件の出所を3分類で区別する（最重要）

**機能を追加・変更するときは、必ず区分を明示する。**

| 区分 | 定義 | 記録すること |
|---|---|---|
| **要求** | 課題文に明記されている | 課題文の該当項目 |
| **派生** | 要求を満たすために技術的に不可避 | **どの要求が成立しないか** |
| **提案・承認済** | 無くても要求は満たせるが、提案し承認を得た上で実装する | **承認の日付** |

**判定基準**: 「これが無いと、どの要求が成立しないか答えられるか」
答えられる → 派生 / 答えられない → 提案・承認済

**禁止事項**

- **承認を得ていない機能を実装しない。** 必要と判断したら、まず提案して承認を得る
- **承認を得ていないものを「提案・承認済」と書かない。** 事実に反する記録になる
- 区分を空欄のまま [機能一覧](docs/features.md) に追加しない

### 2. 認可はサーバー側が根拠

画面上でボタンを隠すのは UX のための配慮であり、**権限の根拠ではない**。

- すべての権限判定を**サーバー側で検証する**
- **プライベートチャンネルは、一覧・検索・ファイル配信・WebSocket 配信のすべてで非参加者に渡さない**
- **例外はオーナーの管理範囲だけである。** オーナーは参加していないプライベートチャンネルの
  **`id` / 名前 / 種別 / 参加者数 / アーカイブ済みか と参加者一覧は取得できるが、メッセージ・添付ファイル・未読数・在席は取得できない**
  （[機能一覧](docs/features.md) 3.1「オーナーには、管理のためのチャンネル一覧を返す」の決定）。
  **この例外が及ぶのは「一覧・取得 API」だけであり、他の経路には及ばない**
  （「一覧・取得 API」は [REVIEW.md](REVIEW.md) 2.1 の表の経路名で、この節の列挙の「一覧」と同じ経路である）
- 存在を隠すべきものは **404**、権限不足は **403** を返す。
  **応答の本体も揃える**——404 を返しても、本文が「権限がありません」であれば存在を認めている

### 3. リアルタイム配信のイベント型は1箇所で定義する

WebSocket のイベント定義（8種類）をフロントとバックで二重に書かない。
共有の型定義から参照する。**2箇所に書くと、食い違いが実行時まで露見しない。**
**配信イベントでない、クライアントとサーバーの間でやりとりするもの（入室要求・退室要求とその acknowledgement、`@here` の受け取りの返事など）の名前と型も、同じ共有の型定義に1箇所で置く**（配信イベントの一覧には足さない。[機能一覧](docs/features.md) 9.2）。

### 4. 判断の代償を書く

機能を落とす・方針を変えるときは、**その帰結を文書に明記する**。

例: 「メール送信をしない」→「パスワードを忘れたら復旧できない」

「実装しなかった」ではなく「**判断して外した。その代償はこれである**」と記録する。

---

## 動き方

**ここに書くのは、レビューの巡回に限らず全作業に効くものである。**
**「開発フロー（必須）」の下が巡回専用というわけではない。** そちらには
**全作業に効くものが混ざっている**——権限の範囲の確認（0）とイシュー作成（1）は
実装フェーズのすべての作業に効き、「統合の理由の置き場」の表と
「なぜ `REVIEW.md` 側に置くのか」は、**この節が規則の実体として指している。**
**「下は巡回のときだけ読む」と扱わない。**

### 手が止まったら確認する

**手が止まったら、まず「止まっているもの」と「動けるもの」を分ける。**
**止めているものが何かを1文で書き、その1文が及ぶ範囲だけを、止まったものとして扱う。**
**止まった側は背景に投げ、動けるものへ移る。動けるようになったら即再開する。**
**動けるものが1つも無いときだけ、前景で待つ。**

**原因は問わない。** 外部サービスの停止・CI の障害・レビュー待ち・依頼側の判断待ち・
approach の行き詰まり・データの不足——**すべて同じ入口を通る。**
**原因を列挙しない。列挙しなかった原因で同じことが起きる。**

**「止めているものを1文で書く」が要である。** 書くと範囲が出る。
「CI が落ちている」ではなく「**止まっているのはマージだけである**」まで書けば、
作業が止まっていないことがその場で分かる。

**動けるものを探す先。**

| 確認先 | 見つかるもの |
|---|---|
| **オープンな PR**（`gh pr list`） | レビューが完走しているのに未処理のもの、コンフリクトで止まっているもの |
| **オープンなイシュー**（`gh issue list`） | 着手すると決めたのに手をつけていないもの |
| **作業ツリー**（`git status` / 未 push のブランチ） | コミット・push の漏れ、ブランチの取り違え |
| **`.claude/records/tasks.md`** | **会話の中で立ち上がったタスク**——イシューにするほどでもないが、忘れると落ちるもの |
| **背景に投げた待ち** | 投げた先が終わっていないか。投げっぱなしになっていないか |

**「動けるものが無い」の判定は、この表を引くこと自体である。引かずに「無い」と判断しない。**

**前景で待つときは `until` で完走を待ち、ターンを閉じない。**
手が空いたのに報告を書いて締めると、**そこで止まる。** 背景タスクの完了は、
次に人が発言するまで届かない。**締めることが停止そのものである。**

**会話で「これをやる」と決まったら、その場で `.claude/records/tasks.md` に1行書く。**
終わったら `[x]` にするか消す。**消し忘れても害はない**——確認したときに、終わっていると分かれば済む。

**この確認で拾えないもの**: **どこにも書かなかったもの。**
**残っていないものは守れない。** 会話は圧縮されて失われるが、ファイルは失われない。

### 同じ approach の変種を繰り返さない

**2回目で結果が変わらなければ、approach そのものを疑い、切り分けの結果を報告する。**

**これは「いつ止まったと判定するか」の基準であり、上の「手が止まったら確認する」の入口になる。**
変種を試し続けている間は「動いている」と錯覚するが、**切り分けが済んだ時点で、その approach は止まっている。**

**実測は [docs/review-criteria-record.md](docs/review-criteria-record.md) 16 節にある**（`multer` の `overrides` を6回試し、切り分けは3回目に出ていた）。
**同 7.4「3巡続けて足し漏れたら、足していく形そのものを疑う」の、作業側での形でもある**——あちらは足していく形、こちらは approach である。

### 選択肢は、実現手段を引用できるところまで作ってから出す

**依頼側に判定を仰ぐ前に、その問いの前提が確認済みかを確かめる。**
選択肢の一方が**調べれば消える**なら、それは判定を仰ぐ問いではない。
**確かめずに選択肢の形にして渡さない。** 判定を仰ぐのは、**確認したうえで残った割れ**だけである。
確認できなかったときは、**何を確認できなかったかを明記して**渡す。
**これは問いの前提の話である**——下の「選択肢の実現手段」は別で、
**そちらは引用できないものを並べない**（明記して渡す、ではない）。

**実測は [docs/review-criteria-record.md](docs/review-criteria-record.md) 12 節にある**
（PR #96 の「Valkey が停止したとき在席経路をどうするか」の二択。**一方は Socket.IO の公式文書を1回引けば消えた**）。

**加えて、残った選択肢の中身が詰まっているかを確かめる。**

> **選択肢を書くとき、各選択肢の実現手段を、このセッションで自分が開いた出力から1つ引用できるか。**
> **引用できないものは未確認である。**「たぶんできる」で選択肢に並べない。

**「調べたか」ではなく「引けるか」。** 取りに行っていても読み落とせば同じことが起きる。
**引用を強制すると、読み落としが検査に掛かる。**

**この前提条件は、依頼側の判断を待って止める・渡す、この文書のすべての箇所に及ぶ。**
**範囲は語句ではなく行為で決まる**——「依頼側に仰ぐ」と書いていなくても、
**「判断が人に属する」として作業を止めるとき**にも当たる。
以降で触れている箇所は言い換えであって、適用先の全部ではない。

### 作業側の停止条件

**「筋が通った」ではなく、項目が埋まったら止まる。**

1. 守るべき性質を1文で書けているか
2. 同じ規則・記述が当たる箇所を**列挙し切った**か（`grep` の結果を根拠に）
3. 性質を破る側を**全方向**で壊して、落ちることを見たか
4. 足した文章は**不変条件の記述だけ**か（経緯・理由は「統合の理由の置き場」の表に従う）
5. **確認していないものを列挙した**か

**2 が破れるのは、`grep` を引かないときではなく、引く範囲の切り方を誤るときである。**
**引く語は、1 の「守るべき性質を1文」から取る。**
**拡張子・変数名・自分が覚えている場所から取らない**——それらは「規則が当たる範囲」ではなく
**その時点で目に入る形**であり、**切った分だけが漏れる**（実測: 1日で4回。#199）。

| 切り方の誤り | 実測 | 代わりに引くもの |
|---|---|---|
| **拡張子で切る** | `'*.md'` で引き、参照実装の `.ts` 2箇所が残った（#83） | **追跡下の全ファイル**（`git ls-files -z \| xargs -0 grep -n …`）。絞るのは、**絞ってよい理由を書けるときだけ** |
| **変数名で切る** | `grep 'in_sec'` で引き、変数名が `in_block` の1箇所が残った（#44） | **性質を述べる語**。名前が揃っていないなら、**当たる関数を数え上げる** |
| **決定を書き換えるとき、逆を述べている側を引かない** | SQL を直し、逆の内容を述べた記述2箇所が残った（#191） | 「足す」と決めたなら「**足していない**」「**書いていない**」「**条件にしない**」も引く |
| **同じ push で自分が足したものを数えない** | 呼び出し6箇所を直し、**同じ push で足した7箇所目**が残った（#44 第6巡） | **直した後にもう一度引く**（「台帳の運用」4） |

### 手戻りできる形にする

**修正は、後からより良い方法へ戻れる形で行う。** 戻れないと、より良い方法が見つかっても採れない。

- **束ごとにコミットを分ける**（push は1回でよい）。台帳に**コミットの SHA を書く**。
  これで手戻りは `git revert <SHA>` の1操作になる。規則単位で適用先全体に当てる方針と噛み合う——
  適用先が広くても1つのコミットに収まっていれば、戻す単位は1つである。**ブランチは消さない**（SHA を辿るため）
- **戻せなくなる操作の前に、必ず報告する**——実データベースへのマイグレーション適用、デプロイ、公開 API の確定、データ移行。
  適用済みのマイグレーションは編集せず、打ち消しは新しいマイグレーションで行う
- **より良い方法に気づいたら、できる範囲で直し、手戻りできる形にしたうえで報告する。**
  **これは提案であって作業報告ではない。** 置き場は**環境で決まる**（開発フロー 0 で確認する）。

  | 環境 | 提案の置き場 |
  |---|---|
  | **一人で開発している** | **イシューにしてよい。** 追跡され、漏れがなく、別の環境にも残る |
  | **複数人で作業している** | **ローカル（`.claude/records/`）に一時的に置き、人の確認を得てからイシューにする。** イシューは全体への報告になるため |

  **ローカルに置く場合の代償**（上記 4）: `.claude/` は `.gitignore` 済みで追跡されない。
  **その機械にしか無く、壊れれば消え、他の人には届かず、次のチェックアウトにも残らない。**
  一時的な保持であり、**確認を得たら速やかにイシューへ移す**こと。

**作業報告と提案は別物である。** 誰がどう考えても必要な変更・作業（🟡 の記録、テスト不足、打ち切りで残ったもの、
着手するタスク）は**イシューにして手をつけてよい**。より良い方法・こう変えた方がよい、は**提案**で、人の確認を経る。

### 台帳の運用

**これは記録の付け方の規則であり、このアプリケーションの実装とは関わらない。**

**実測は [docs/review-criteria-record.md](docs/review-criteria-record.md) 7.3 にある**（1・2 は r7・r9、3 は PR #165 の第6巡）。

**台帳（PR 本文の「確定した前提」）の読み手と書き方は「統合の理由の置き場」の表にある。**
**扱いを次に定める。**

1. **台帳の適用先に挙げたら、同じ push のうちに実文へ当てる。**
   **挙げただけのものは「適用済み」に見える**——次に読む人はそこを直さず、同じ指摘が再び出る
2. **台帳の事実誤りは、重大度によらず気づいた時点で直す。**
   **台帳が判断の基準になる以上、誤ったまま次へ渡さない**
3. **新しい内容を足すときは、同じ規則の束が既に持っている適用先の一覧を引き直す。**
   **束の欄に2箇所と書いてあっても、足すときにその欄を見なければ片方に届かない**
4. **適用先の欄には、引いたコマンドと、直した後の2つの件数を書く**——
   **引き直したときのヒット数**と、**そのうち規則を満たしていないものの件数**。
   **後者が 0 でないなら、まだ列挙し切っていない。**
   **前者は、足す方向の修正では 0 にならない**——引き直せば適用先がそのまま返るためである
   （[REVIEW.md](REVIEW.md) 9.4「自己生成」のように**消す方向**で直したときは、
   ヒット数そのものが 0 になってよい）。
   **`grep` は直す前にしか引かれないことが多く**、直した後に同じコマンドを引き直さないと、
   **範囲の切り方の誤りがその push に残る**
   （実測: 1日で 5 件のうち、後を引いた 1 件だけが 1 巡で閉じた。#199）

### 文書どうしが食い違ったら

**[REVIEW.md](REVIEW.md) 9.3 が定める。ここには書かない**——理由は、下の `### AI レビューの指摘に対応する` にある**「なぜ `REVIEW.md` 側に置くのか」**と同じである。

---

## 開発フロー（必須）

0. **作業を始める時点で、権限の範囲を確認する**——**マージ**（どこまで進めてよいか。環境と可逆性で決まる）と
   **イシュー作成**（作業のイシューを AI が起票してよいか。**提案をイシューにしてよいか**——
   一人なら可、複数人なら人の確認を経てから。現場によっては全件確認が要る）。
   許可は環境に紐づき、人が増える・本番に影響する・作業を再開する、のいずれかで再確認する。
   **許可なしに既定として扱わない**
1. 作業開始前に必ず GitHub イシューを作成する（上の 0 で許された範囲で）
2. ブランチを作成してから作業する。命名: `feature/#<イシュー番号>-<短い説明>`（修正は `fix/`）。
   **ブランチ名は英数字と `/` `-` `#` だけで書く。`<短い説明>` に日本語や他の記号を使わない**——日本語を含むと `review` が実行を拒否する（実測）。英数字と `/` `-` `#` はアクションの許可集合の内側にある（[README](README.md)「既知の制約」）
3. **`main` ブランチへの直接プッシュは禁止。** 変更は必ず Pull Request 経由
4. **1つの PR には1つの目的のみ。** 無関係な変更を混ぜない。
   **「1つの規則・決定を、その適用先全体に当てること」も1目的である**（[REVIEW.md](REVIEW.md) 9.3）。
   ファイルや機能を跨いでも、当てている規則が1つなら1目的として扱う。
   **適用先が PR の目的を超える場合は、規則単位の別の PR に分ける**
5. 機能実装には必ずテストコードをセットで作成する（テストなき実装は未完成）
6. **CI 4本（`code` / `docs` / `audit` / `review`）の完走を確認する。**
   **`review` の `success` は「完走した」であって「指摘なし」ではない**——指摘を読んで仕分けるまでが 6 である。
   **マージするかどうかは、上の 0 で確認した権限の範囲による。範囲の外なら止めて聞く**
7. **マージされたら** `main` に戻り `pull` して最新化してから次の作業に入る。
   **自分でマージしたとは限らない。** 0 の範囲の外なら、人がマージするまで待つ

### AI レビューの指摘に対応する

**直すのは実装側であり、CI の中のレビュアーではない。**
レビュアーに修正と push を許すと、次のレビューで**自分が書いたコードを自分でレビューする**ことになり、
「こう作ったはず」という文脈が働いて見逃す。だからワークフロー側では `Bash` を禁止している。

#### 直すのは 🔴 と、認可・秘密に関わる 🟣 だけである

**判定規則は [REVIEW.md](REVIEW.md) 9章「判定規則」にある。ここには書かない。**
基準の一覧（A〜E）・🔴 と 🟡 の閾値・整合性の単位・巡回の形・打ち切りの条件は、すべてそこにある。

**認可・秘密に関わる 🟣 は、この文書の以降のすべての記述で「🔴」と読む**（[REVIEW.md](REVIEW.md) 1章）。
**箇所ごとに書き足さない。** 足し漏れた箇所がそのまま穴になる。
**以降で 🟣 に触れている箇所は、この規則の言い換えであって、適用先の全部ではない。**

**なぜ `REVIEW.md` 側に置くのか。** CI のレビュアーは、**このファイル（`CLAUDE.md`）を
PR の版で読めない。** ワークフローがルートの `CLAUDE.md` を差し替え、PR の版を
`.claude-pr/CLAUDE.md` へ退避するためである（#46 で確認）。
**ここに定義を置くと、`CLAUDE.md` を変更する PR で、その PR 自身に適用できない。**
`REVIEW.md` は PR の版がそのまま読まれる。レビュアー自身が重大度と基準の記号を付ける以上、
判定規則はレビュアーが読む文書に無ければならない。

> ワークフローの指示に「`.claude-pr/CLAUDE.md` を読め」と足す手もあるが、採らない。
> **`.claude-pr/` はアクションの内部実装で文書化されておらず、更新で黙って変わりうる。**
> そして**レビュー観点（`REVIEW.md`）は、元から PR の版で読まれる前提の文書**であり、
> レビュアーに効かせたい規則はそこに置くのが筋である。

**🟡 は直さない。その巡回でイシューに記録し、節目でまとめて片づける**（例外は [REVIEW.md](REVIEW.md) 1章が定める設計の提案①②だけである）。
「本物の指摘だから直す」を続けると終わらない。**実測で、242 巡回・🟡 661 件のうち
実装が壊れるものは 38% であり、🟡 が 0 件で終わった巡回は 5% しかなかった。**

**「🔴 か 🟡 か」が割れるものだけ、依頼側に判定を仰ぐ。** 記号どおりの 🔴 は、確認を待たずに直してよい。
**仕分けを確定するのは依頼側である**（REVIEW.md 9.1 の E）。

**仰ぐ前に確かめることは「選択肢は、実現手段を引用できるところまで作ってから出す」にある。**

#### 修正が次の指摘を生む6条件

**直し方が悪いと、🔴 だけでも連鎖する。** 全履歴の巡回を追うと、長引いた PR はすべて次のどれかだった。

| # | 条件 | 実例 |
|---|---|---|
| 1 | **規則の適用先を網羅せず、指摘された1箇所だけ直す** | 退会＝論理削除という1つの決定が、経路を1つずつ変えて12巡した |
| 2 | **同じ記述が複数箇所にあり、一部だけ直す** | 同じ条件文が4箇所に複製され、参照に寄せた側と残った側が食い違った |
| 3 | **修正と同じ push で、レビューされる差分に説明文を足す** | 足した注記が「向きが逆」「範囲が過大」「前提が同じ push で消えた」と次巡の指摘になった |
| 4 | **症状を直して、性質（不変条件）を固定しない** | 大文字小文字の照合を片側だけ固定し、次巡で「もう片方を落としても落ちない」が出た |
| 5 | **PR の途中で設計判断を変える** | オーナーの例外を PR の中で決めた結果、4つの PR に跨って連鎖した |
| 6 | **参照・引用を書くとき、指す先の実文と突き合わせない** | 引用した見出しが実文と一致せず、その文字列では引けなかった |

**3 は「理由を残すな」ではない。置き場の問題である。** 下記「統合の理由の置き場」を参照。

#### 1巡回の手順

**複数の指摘は、束ねて1つの成果物にする。** 別々に直すと、片方は直り片方は壊れる状態が繰り返される。

1. **台帳を読む**（指摘を読む前に）。セッションが替わっていても、ここから再開できる
2. 指摘を全部読む（詳細分と、上限を超えて見出しだけで返った分）
3. **規則・決定・記述で束ねる。** ファイルや行では束ねない
4. 束ごとに、**台帳の規則の名前**と照合する（9.4「同じ束」の判定）
   - **一致する束がある** → その束の「出た巡回」に今回を足す。**打ち切りは回数ではなく理由で決める**（[REVIEW.md](REVIEW.md) 9.4）
   - 一致しない → 新しい束として台帳に足す
   - **箇所が違っても、記述が新しくても、規則が同じなら同じ束である。**
     「新規かどうか」で数えない（適用先の漏れと自己生成が新規に見えて、5巡以上での要因の報告が発動しない）
5. 重なりが有る束は、**台帳の不変条件を前提に**、新しい部分だけを切り出す。
   前の形を変える必要があるなら**前提の衝突**であり、**直さずに依頼側へ上げる**
6. 束ごとに、**守るべき性質を1文で書き**、**適用先を `grep` で列挙し**（**引く範囲の切り方は「作業側の停止条件」2 の表による**）、**1つの変更**を設計する
7. **性質を破る側を全方向で壊して、落ちることを見る**
8. **台帳を更新し、束ごとにコミットを分けて理由の全文をコミットメッセージに書き、1回で push する**

#### 統合の理由の置き場

**理由は必ず残す。置き場だけを分ける。**

| 置き場 | 何を置くか | 誰が読むか |
|---|---|---|
| **PR 本文の「確定した前提」（台帳）** | 束ごとに、**規則の名前**／不変条件1文／**適用先の一覧（引いたコマンドと、直した後の2つの件数）**／統合した指摘／**採った形と、採らなかった形の理由**／**その束が出た巡回**（r1 → r2 …）／検証／未確認。**扱いは「台帳の運用」による** | **次に読む人が最初に開く。レビュアーも読む** |
| **コミットメッセージ** | 統合の理由の全文 | `git log` で永久に辿れる |
| **ソース** | **不変条件だけ。** 経緯と理由は書かない。**時期は下の例外表による** | 次に触る人 |
| **[docs/review-criteria-record.md](docs/review-criteria-record.md)** | 汎用化に要りうる実測・経緯・判断。**判定基準は同文書冒頭「何を置くか」**（迷ったら残す） | 別プロジェクトで土台を作る人 |

**ソースに不変条件を書く時期は、既定では仕上げの1回である。**
動作を直しながら「なぜ直したか」を書くと、**調べ切る前に説明を書くことになる。**
実際に、誤った理由付けが3コミット・約11時間生き延び、8巡目に撤回された
（`.prettierignore` の Prettier の挙動についての記述。**結論と手当は正しく、理由だけが誤っていた**）。

**例外は次の表のものだけ。その巡回でソースに書く。**

| 種類 | なぜ延期できないか |
|---|---|
| **規則・手順を定めた文書（`REVIEW.md` / `CLAUDE.md` / `scripts` のコメント）の食い違いの訂正** | **規則文書の中身は「なぜそう決めたか」そのものであり、直すこと自体が不変条件を書くことである。** 延期すると、手順 3 の「🔴 はその巡回で直す」が実行できない |
| **踏むと壊れる警告** | 次に触る人が壊す。担当が引き継がれる前提の作り方をしている |
| **機能の区分の記録**（[機能一覧](docs/features.md)） | 「1. 要件の出所」が実装と同時を求めている |
| **誤ったコメントの訂正** | 誤った理由付けを残すと、次に触る人を誤らせる |

**例外は「消す・短くする」方向にだけ広げてよい。** 書き足す方向に広げると、
上の「調べ切る前に説明を書く」に戻る。

**レビューされる差分に説明文を足さない**（上記の条件3）。
実際に、修正と同時に足した説明が次の巡回の指摘になった例が繰り返し出ている。

#### 手順

**0. PR の説明に、対象の機能 ID を書く。**
`機能 ID: F-05, F-29` または `機能 ID: なし（運用文書・CI・scripts）`。
**A〜E は、どの PR でも**すべて**当てる。範囲を宣言で絞らない**（REVIEW.md 9.4）。
後者で機能要件（受け入れ条件・区分・性能の目標値）の指摘が出ないのは、
**見ないからではなく、当たる対象が無いから**である。
**「適用する基準」は書かない。** 基準は 9.1 の許可列挙で決まっており、**PR 側で絞れるものではない。**
書ける形にすると、**無条件であるはずの B を宣言で外せてしまう。**
**B の実体は REVIEW.md 9.1 の表であり、ここで数え直さない**（数え直した版が 9.1 から漏れる）。

1. CI のレビューが終わるまで待つ
2. 指摘を読み、**規則の名前で束ねる**（上記「1巡回の手順」3。**記号では束ねない**——
   記号は5つしかなく、束の粒度が潰れて「同じ束」の数え方が変わる。[REVIEW.md](REVIEW.md) 9.4）。
   **「🔴 か 🟡 か」が割れるものは、自分で決めず依頼側に申告する**
3. **🔴 と、認可・秘密に関わる 🟣 を、上記「1巡回の手順」で直す。それ以外の 🟣 と 🟡 は
   その巡回でイシューに落とす**（🟣 の扱いは [REVIEW.md](REVIEW.md) 1章・9.4。
   **認可・秘密の 🟣 をイシューに送ると、認可の穴を残したままマージできてしまう**）
   （テスト項目の不足は、**必須項目と、実装を読んで誤りが分かるものを除き**、
   **テスト追加のイシューに集約**する。[REVIEW.md](REVIEW.md) 6章の2つの条件）
4. **台帳（PR 本文の「確定した前提」）を更新してから push する。**
   レビューの結果は同じコメントに上書きされ、前の巡回の内容は次の巡回で消える。
   **台帳が、次の巡回と打ち切り判定の基準になる**
5. **仕上げは1回だけである**（[REVIEW.md](REVIEW.md) 9.4「巡回の形」2。**条件も回数もそこが持つ**）。
   **設計の提案①②（揺れ・重複）を実装に関わるものと文書の修正で分ける扱いは、
   [REVIEW.md](REVIEW.md) 1章・9.4 が持つ。ここには書かない**
6. **CI 4本の完走を確認して報告する**（基準は開発フロー 6 と同じ）。
   **マージは巡回に含まれない**（REVIEW.md 0 / 9.4）
7. **「何を直したか」「何をイシューに落としたか」「何を対応不要と判断したか」を報告する。**
   **より良い方法に気づいていれば、それも報告に含める**（提案として。**置き場は開発フロー 0 で確認した環境による**——「動き方」の「手戻りできる形にする」）

**打ち切りの条件は [REVIEW.md](REVIEW.md) 9.4 が持つ。ここには書かない**——上の「直すのは 🔴 と、認可・秘密に関わる 🟣 だけである」が、判定規則の5つを名指しで手放している。
**巡回は打ち切りの条件を満たすまで止めない。** 判断が要るときは 9.4 を開く。

#### 先送りしたものを片づける時点

**イシューに落としただけでは、直したことにならない。**
先送りしたもの——**🟡 として記録したものと、認可・秘密に関わらない 🟣、打ち切りで残ったもの**——は、
次の2つの節目でまとめて片づける。

- **実装フェーズに入る前**
- **提出前**

**そこでは新しい機能を作らず、溜まったイシューを閉じることだけを行う。**
**この文書群そのものが成果物である**（スクール課題）ため、
文書の食い違いを残したままにはしない。

**マージは上のループに含めない。** 可否を判断するのは人間である（[REVIEW.md](REVIEW.md) 0）。
**手順 6 が「許された範囲の中でだけ行う」と書いているのは、その範囲を人が事前に決めているためである**
（開発フロー 0）。範囲を決めずに巡回の中でマージしてはならない。

### 開発方式はテスト駆動開発（TDD）

**実装より先にテストを書き、失敗すること（Red）を確認してから実装する。**
後から書いたテストは「通ることを確認しただけ」になりやすく、
**壊れたときに落ちるかを検証できていない。** 詳細と適用範囲は
[要件定義書](docs/requirements.md) 4.8 に記す。

### 必ずテストを書く箇所

以下は**実装漏れが起きやすく、漏れたときの影響が大きい**ため、テストを必須とする。

- **検索がプライベートチャンネルを漏らさないこと**
- **添付ファイルの配信とアップロード用の署名付き URL の発行が、非参加者を拒否すること**
- **WebSocket が非参加者にイベントを配信しないこと（非参加者をチャンネルの部屋に入れないこと、参加者でなくなった接続を部屋から外すこと、利用者の部屋へ送る値を、受け取る資格の無い利用者（DM なら当事者でない利用者・そのワークスペースのメンバーでない利用者、チャンネルに属する値ならそのチャンネルの参加者でない利用者）に届けないことを含む。機能一覧 5.2・9.2「部屋（Socket.IO の room）」）**
- **WebSocket が許可外の Origin からのハンドシェイクを拒否すること**
- **Markdown が HTML として解釈されないこと**（XSS）
- **自分以外のメッセージを編集・削除できないこと**
- **メンバーがオーナー専用の操作を実行できないこと**
- **オーナーが、参加していないプライベートチャンネルのメッセージ・添付ファイル・未読数・在席を取得できないこと**
- **退会済みのトークンが、読み取り・書き込み・WebSocket のいずれでも拒否されること**

## コミット規約

- メッセージは日本語。形式: `<種別>: <変更内容の要約>`
- 種別: `feat` / `fix` / `docs` / `test` / `chore` / `refactor`

**Dependabot が作るコミットは英語になる**（`chore: bump X from 1.0 to 1.1`）。
人が書くものではないため、この規約の対象外とする。
**メッセージを日本語にするために自動更新を止める、という順序にはしない。**

**現在この経路が残っているのは GitHub Actions の更新だけである。**
npm の版更新は止めた（[README](README.md) の「依存の版を上げない方針」）。
**止めた理由は、実際に3件中3件が受け入れられなかったからであり、
コミットメッセージの言語とは無関係である。** 上の順序は変えていない。

## 禁止事項

- `.env` の値をコミット・出力しない（存在と変数名までに留める）
- パスワード・トークン・セッション情報をログ出力の経路に渡さない
- `git push --force` 系、`git reset --hard`、`rm -rf` は使わない

---

## 現在の状態

**要件定義フェーズは完了。実装は進行中。**

| フェーズ | 状態 |
|---|---|
| 要件定義 | **完了**（2026-09-04。総点検の指摘22件への対応を反映済み） |
| **AI コードレビューの観点** | **完了**（[REVIEW.md](REVIEW.md)） |
| **AI コードレビューの設定** | **完了**（[claude_code_review.yml](.github/workflows/claude_code_review.yml)。導入手順と制約は [README](README.md)） |
| **ドキュメント検査の CI** | **完了**（[docs.yml](.github/workflows/docs.yml)。検査そのものの検査を69通り含む） |
| lint・型チェック・ビルド・テストの CI | **完了**（[ci.yml](.github/workflows/ci.yml)） |
| 依存の脆弱性検査 | **完了**（[audit.yml](.github/workflows/audit.yml)。**脆弱性に気づく経路はこれだけである**） |
| 依存の更新方針 | **完了**（[dependabot.yml](.github/dependabot.yml)。**npm の版は固定し、GitHub Actions の更新のみ受け取る**。脆弱性検査ではない）。**固定するのは「新しい版が出たから上げる」だけであり、脆弱性を塞ぐ更新は取り込む**（[README](README.md)「依存の版を上げない方針」） |
| プロジェクトの雛形 | **完了**（apps/api / apps/web / packages/shared） |
| 開発環境の Docker（DB・Redis） | **完了**（[compose.yaml](compose.yaml)。**Docker に入れるのはミドルウェアだけ**。api / web はホストの Node で動かす） |
| 実装 | 実装中 |
