# apply のときに渡す値（#452）。秘密ではないが、使う環境ごとに決まるためリポジトリに書かない。
# 踏むと壊れる: このファイルにも main.tf の冒頭の検査の条件が掛かる（apps/api/src/config/api-config-infra.test.ts）。

# 踏むと壊れる: **秘密の値を入れ替える apply では、この2つに「いま動いている値」をそのまま渡す**
# （要件定義書 4.2「秘密の値が漏れた疑いがあるとき」の共通の前置き。採り方もそこにある）。
# 新しい値を渡すと、入れ替えの範囲を超えて結果が変わる——image_tag なら**別のイメージを本番へ出し**、
# alarm_email なら**購読が作り直されて、確認のメールを開くまでアラートが届かない**
# （その手順の Valkey の箇条は「入れ替えの間はメトリクス欠損が鳴りうる」を代償の一部として当てにしている。
# 鳴るのは欠損が評価期間だけ続いたときで、作り直しがそれより短ければ鳴らない。
# RDS と JWT_SECRET の箇条の 5xx 率は、要求が来ていなければそもそも鳴らない——alarms.tf の treat_missing_data）。

variable "image_tag" {
  description = "ECR に push した api のイメージとマイグレーション用のイメージのタグ。同じソースから作った2つのイメージに同じタグを付ける"
  type        = string
}

variable "alarm_email" {
  description = "アラートの通知先のメールアドレス（要件定義書 4.2「アラート」。購読の確認は依頼側が行う）"
  type        = string
}
