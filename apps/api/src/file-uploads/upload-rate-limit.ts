/**
 * アップロードの発行と確定の上限（利用者単位で 10 分に 30 回。機能一覧 11.1。実装時に決めた値）。**ルートごとに別の枠で数える**
 * （`UserRateLimitGuard` の既定。アバターと添付、発行と確定がそれぞれ別の枠）。
 *
 * 値の理由: 1 通の投稿に数個の添付を付けて何通か続けて上げても届く回数にし、**発行1回で S3 に書けるのは1版（確定の後にもう1回）**のため
 * （11.1 の条件付き書き込み）、S3 への書き込みの回数を利用者1人あたり 10 分に 60 版程度に抑える。確定は隔離用のキーを読んでコピーと削除を行う
 * 最も重い段であり、発行と同じ値にする（確定は発行した識別子ごとに1回しか S3 を触らないため、実際の重さは発行の回数で抑えられる）。
 */
export const UPLOAD_ISSUE_LIMIT = { limit: 30, ttl: 10 * 60 * 1000 } as const;
export const UPLOAD_COMPLETE_LIMIT = { limit: 30, ttl: 10 * 60 * 1000 } as const;
