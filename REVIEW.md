# コードレビュー観点

AI によるコードレビューに与える指示。人間のレビューでも同じ観点を使う。

**このファイルは「何を見るか」を定義する。** [CLAUDE.md](CLAUDE.md) はプロジェクトの
運用ルールを定義するもので、役割が違う。両方が読まれる前提で、重複を避けて書く。

---

## 0. このプロジェクトでのレビューの位置づけ

**AI レビューは指摘するだけであり、承認の責任は持たない。** マージの可否は人間が判断する。
「AI が OK と言ったからマージ」は成立しない。

**CI（lint・型チェック・テスト）で機械的に判定できるものは、レビューの対象外とする。**
機械が確実に判定できることを AI に投げるのは非効率であり、指摘もぶれる。

---

## 1. 重大度の定義

| 記号 | 重大度 | 意味 | 扱い |
|---|---|---|---|
| 🔴 | **重大** | 本番で壊れる、または情報が漏れる。マージ前に直すべき | 必ず報告する |
| 🟡 | **要修正** | 動くが問題がある。直したほうがよい | 報告する |
| 🟣 | **既存の問題** | この変更が原因ではないが、周辺に見つけた問題 | 報告する（この PR で直す必要はない） |
| ⚪ | **好みの問題** | 動作に影響せず、書き方の好みに属する | **報告しない** |

**⚪ は報告しない。** 好みの指摘が混ざると、🔴 が埋もれて読まれなくなる。

**🔴 に上限は無い。見つけた分をすべて書く。**
上限がかかるのは 🟡 だけである。ここを書き分けないと、🔴 にも上限があると誤読される。

**1回のレビューで、詳細まで書く 🟡 は最大5件までとする。**
上限は、詳細が多すぎて **🔴 が埋もれるのを防ぐため**である
（🔴 は必ず先頭に書く。「報告の形式」に順序を定めている）。

**上限に入れる順序を決める。段階1に該当する 🟡 を先に入れる。**
段階1に該当しない 🟡（性能・要件3分類など）で上限が埋まると、
**実装側が直すべきものが上限の外へ押し出され、段階1が終わらない。**
該当の定義は「段階の定義」にある。

**上限を超えた分は、件数だけでなく見出しを1行ずつ書く。**

```
他に 2 件（見出しのみ）:
  6. compose.yaml のコメントが、存在しない depends_on を根拠にしている
  7. README の「（後述）」が、この文書の中で解決しない
```

**件数だけでは、次の巡回に持ち越せない。**
レビューは巡回ごとの記憶を持たないため、**超えた分が何だったかを誰も知らないまま消える。**
次の巡回で同じものが再び見つかる保証も無い（実測では、前後の巡回で指摘が重複しなかった）。

見出しがあれば、実装側は次の3つができる。

1. **超えた分をイシューに落とす**（記録が残る）
2. **次の巡回で、その分を優先して直させる**
3. **総数の比較に加えて、同じ指摘が残っているのか入れ替わったのかを区別する**

**見出しだけの分は、その巡回では直さない。**
このファイルの「報告の形式」が求める「なぜ問題か」「どう直すか」が
付いていないため、**実装側が判断できない。** イシューに落とし、次の巡回で詳細を受け取る。

**ワークフローの指示は、出力の作法についてこのファイルを参照するだけである。**
同じ事項をワークフロー側にも書くと、どちらを読むかで出力が変わる。
実際に「件数だけ伝える」という古い指示が残っていて食い違った（#62）。
**同じ規則を2箇所に書かない。**

---

## 2. 最優先で確認すること（このプロジェクト固有）

**以下は実装漏れが起きやすく、漏れたときの影響が最も大きい。** ここを最初に見る。

### 2.1 認可 — プライベートチャンネルは4経路すべてを塞ぐ

プライベートチャンネルの内容が非参加者に渡る経路は4つある。**1つでも塞ぎ忘れると要求が成立しない。**

| # | 経路 | 確認すること |
|---|---|---|
| 1 | **一覧・取得 API** | 非参加者に 404 を返すか。403 ではないか（存在を隠す） |
| 2 | **検索** | 検索クエリが「自分が参加しているチャンネルと DM」に限定されているか |
| 3 | **添付ファイルの配信** | **署名付き Cookie が、参加者に対してそのチャンネルのパス配下に限って発行されているか。** 対象パスが広すぎないか（`/*` になっていないか）。**S3 のキーが `workspace/{ws}/channel/{ch}/...` の構造から外れていないか**（キー設計が認可の一部である） |
| 4 | **WebSocket の配信** | 非参加者の接続にイベントが配信されていないか。**ハンドシェイクで `Origin` を検証しているか**（CORS は WebSocket に効かない） |

**特に 2 と 3 は漏れやすい。** 画面に出さない実装をしていても、API が返していれば漏洩である。

**加えて、オーナーの閲覧範囲を確認する。** オーナーは参加していないプライベートチャンネルの
**`id` / 名前 / 種別 / 参加者数と参加者一覧は取得できるが、メッセージと添付ファイルは取得できない**
（[機能一覧](docs/features.md) 3.1「オーナーには、管理のためのチャンネル一覧を返す」）。
この境界を越えていないか。**例外が及ぶのは経路1だけである。**

### 2.2 認可はサーバー側にあるか

- **フロントエンドでボタンを隠すだけになっていないか**
- 「自分のメッセージのみ編集・削除できる」がサーバー側で検証されているか
- 「オーナーのみが招待・キック・チャンネル操作を行える」がサーバー側で検証されているか
- **クライアントから送られた `userId` や `role` を信用していないか**（トークンから導出すべき）

### 2.3 要件の3分類が守られているか

- **承認を得ていない機能が実装されていないか**（[CLAUDE.md](CLAUDE.md) 1）
- [機能一覧](docs/features.md) に無い機能が追加されていないか
- 追加されている場合、区分と根拠が記載されているか

**これは技術的な問題ではなく、要求管理の問題である。** 見つけたら 🟡 として報告する。

### 2.4 WebSocket のイベント型が二重定義されていないか

フロントエンドとバックエンドで、イベント名や引数の型を別々に書いていないか。
**共有の型定義から参照しているか。**

### 2.5 XSS — Markdown が HTML として解釈されないか

- `dangerouslySetInnerHTML` が使われていないか
- サニタイズの設定が外されていないか
- **`javascript:` `data:` などのスキームのリンクが無効化されているか**

---

## 3. セキュリティ

判断基準は **CWE Top 25**、**OWASP ASVS 5.0**、**IPA「安全なウェブサイトの作り方」** に準拠する
（出典は末尾）。以下はこのプロジェクトで実際に起こりうるものに絞った。

| 観点 | 確認すること | 参照 |
|---|---|---|
| **XSS** | 利用者入力が HTML として解釈されないか。Markdown 描画、表示名、ステータス、チャンネル名すべて | CWE-79（2025年 **1位**） |
| **SQL インジェクション** | `$queryRaw` で文字列を連結していないか。**特に検索の絞り込み演算子（`from:` `in:`）と pg_bigm のクエリ** | CWE-89（**2位**） |
| **CSRF** | Cookie を使うエンドポイント（リフレッシュ・ログアウト）に対策があるか | CWE-352（**3位**） |
| **アクセス制御の不備** | 2.1 / 2.2 のとおり | CWE-284 / CWE-639 |
| **リソースの無制限確保** | アップロード、WebSocket イベント、メッセージ投稿にレート制限があるか | CWE-770 |
| **パストラバーサル** | ファイル名を利用者入力からそのまま組み立てていないか | CWE-22 |
| **WebSocket の Origin 検証** | **CSWSH 対策があるか。WebSocket は同一オリジンポリシーの対象外である** | — |
| **認証情報の露出** | パスワード・トークンがレスポンス、ログ、エラーメッセージに出ていないか | — |
| **エラーメッセージ** | 認証失敗で「ID が無い」と「パスワードが違う」を区別していないか | — |
| **アップロード検証** | マジックバイトを検証しているか。**拒否リストではなく許可リストか** | — |
| **識別子の推測** | ファイルID・メッセージID が連番になっていないか | — |
| **依存の脆弱性** | 新しい依存を追加する PR で、その必要性が説明されているか | — |

---

## 4. 設計と可読性

判断基準は **Google の Code Review Developer Guide** に準拠する（出典は末尾）。

Google が挙げる観点のうち、**このプロジェクトで AI に見てほしいもの**を選んだ。
スタイルは CI（lint / formatter）が判定するため、レビューでは扱わない。

| 観点 | 確認すること |
|---|---|
| **設計** | その変更はここに属するか。既存の仕組みと整合しているか |
| **機能** | 意図したとおりに動くか。**エッジケース、並行処理の問題を見る** |
| **複雑さ** | 必要以上に複雑になっていないか。**「将来必要になるかもしれない」ための一般化をしていないか**（Google はこれを over-engineering として明確に指摘対象にしている） |
| **命名** | 識別子がその目的を伝えているか |
| **コメント** | **「何をしているか」ではなく「なぜそうしたか」を書いているか。** コードで表現できることをコメントで補っていないか。**位置に依存した参照（行番号・「前の」「後述」・件数）を書いていないか**（「コメントと文書に、位置に依存した参照を書かない」） |
| **一貫性** | 既存のコードのやり方から不必要に外れていないか |
| **ドキュメント** | 挙動が変わったのに [機能一覧](docs/features.md) や [要件定義書](docs/requirements.md) が古いままになっていないか |

#### 段階の定義

**レビューへの対応は2段階に分ける。ここが唯一の定義であり、
[CLAUDE.md](CLAUDE.md)「AI レビューの指摘に対応する」はこれを参照する。**

> **なぜ `CLAUDE.md` ではなくここに置くか。** CI のレビュアーは `CLAUDE.md` を
> PR の版で読めない。ワークフローがルートの `CLAUDE.md` を差し替え、
> PR の版を `.claude-pr/CLAUDE.md` へ退避するためである。
> **`CLAUDE.md` に置くと、`CLAUDE.md` を変更する PR で、その PR 自身に適用できない。**
> このファイルは PR の版がそのまま読まれる。

**「指摘が出なくなるまで回す」は原理的に満たせない。**
レビューは巡回ごとの記憶を持たず毎回まっさらに読み直すため、
**同じ品質でも読むたびに別の指摘が出る。** 実測で 15巡・16巡しても収束しなかった
（9巡で指摘なしに到達した PR もある。**到達することはあるが、確実ではない**）。

**段階1 — 動作・認可・秘密。🔴 と、段階1に該当する 🟡・🟣 が空になるまで回す。**
（🟣 のうち段階1に該当するのは、認可・秘密に関わるものである。下の箇条書きで定める）

- 🔴 はすべて
- 🟡 のうち、**振る舞いが変わる / 認可に関わる / 秘密が漏れうる /
  検査やテストが実際には効いていない**もの
- **書いてある手順が実際には機能しない**ものも含む（成果物が手順である場合）
- **上のどれにも当たらない 🟡 と、🟣 は対象に含めない。**
  どちらも直さないまま毎巡回で再び報告される。**レビューは記憶を持たないので、
  「前回そう判断した」も引き継がれない。** 含めると段階1が終わらない。
  **イシューに落として対象から外す**

  例: 性能の指摘（N+1・`OFFSET`）や、**承認を得ていない機能が実装されている**という指摘は
  🟡 で報告されるが、振る舞い・認可・秘密・検査の有効性のどれでもない。**段階1では対象外とする。**
- **ただし 🟣 でも、認可・秘密に関わるものは段階1に含める。**
  既存の認可の穴は「この変更が原因ではない」（🟣）と「情報が漏れる」（🔴）の両方に当てはまり、
  どちらを付けるかで扱いが変わってはならない。**プライベートチャンネルが漏れる指摘が、
  🟣 と付けられただけで先送りされる経路を作らない。** 迷ったら 🔴 を付ける
- **[機能一覧](docs/features.md) の区分の記載漏れは、段階1に該当する。**
  [CLAUDE.md](CLAUDE.md) 1 が「区分を空欄のまま追加しない」を禁止事項としており、
  段階1でもソースに書く例外に挙げている。**承認の有無（対象外）と区分の記載（該当）を分ける**
- **段階1にも上界を置く。「空になるまで」だけでは、段階2と同じく原理的に終わらない。**
  レビューは記憶を持たず、段階1該当の指摘も読むたびに別のものが出うる。
  **数えるのは段階1に該当する指摘だけである。** 段階1に該当しない 🟣 と、対象外としてイシューに落とした 🟡 は
  毎巡回そのまま再報告されるため、含めると「前の巡回の指摘が残った」状態が常に成立し、
  **打ち切りが原理的に発動しない。**
  **段階1該当のうち、前の巡回の指摘が1件も残らず、新規だけが出る巡回が2回続いたら**、
  直すたびに新しい指摘が生まれる状態に入っている。そこで巡回を続けず、**依頼側が打ち切りを判定する。**
  打ち切りは先送りではない。**残った段階1該当は、別の PR に分けて段階1として直す**
  （🔴 と認可・秘密に関わるものを、イシューに落として済ませることはしない）。
  「2回」は根拠のある値ではなく、1回目で傾向を見て2回目で確認するという運用上の区切りである

**記録**: この規則を入れた PR #51 自身が、段階1で十回を超えて巡回した。
出た指摘はすべて「規則どうしの食い違い」であり、コードの動作の指摘は無かった。
巡回数はここに書かない（この文書の「位置に依存した参照」の規則のとおり、手で書いた件数は
書いた時点から古くなる）。**実数は、そのブランチのレビューのワークフロー実行履歴から数える。**
- **段階1に該当する 🟡 は、上限を超えて見出しだけで返った分も「空」の判定に含める。**
  イシューに落としても段階1は終わらない。対象から外してよいのは非該当の分だけである。
  段階1該当だけで上限を超え続けるなら、**巡回を続けるのではなく PR を分ける**

**段階2 — 文書とコメントの整合。指摘の総数が減っている限り回す。**

- **段階2の最初の巡回は比較の基準であり、そこでは止めない。** 比較は2巡目以降で行う。
  段階1の最後の巡回は表現の指摘を報告していないため総数がほぼ 0 であり、
  **1巡目を「前の巡回」と比べると必ず「増えた」になって、1巡もしないうちに止まる**
- **2巡目以降、前の巡回より減っていなければ、そこで止めてイシューに落とす**
- **段階2で段階1に当たる指摘が出たら、段階1に戻って空にし、段階2を最初からやり直す。**
  **件数の比較もそこで仕切り直す。** 戻る前の件数とは比べない。
  戻ってから直した結果、文書とコメントの整合は変わっているためである。
  **古い件数と比べると、減っていないように見えて早く止まる**
- **比べるのは表示された件数ではなく「見つかった総数」である。**
  🟡 は詳細を書くのが1回5件までで、超えた分は見出しだけが返る。
  **5件は「5件だった」ではなく「上限に当たった」かもしれない。**
  真の件数が 30 → 12 と減っていても表示は 5 → 5 になる。
  **そこで止めると、最も残っているときに止めることになる。** 総数（表示分 + N）で比べる
- **数えるのは段階2の対象（文書とコメントの整合）だけである。**
  🟣 と、イシューに落として対象から外した 🟡 は数に含めない。
  **どちらも直さないまま毎巡回で再び報告されるため、含めると定数として乗り続け、
  直せる分を全部直しても総数が下げ止まり、「減っていない」と判定されて止まる。**
  超えた分も見出しで返るので、除外した分は識別できる
- **上限に当たり続ける場合は、巡回を続けるのではなく PR を分ける**

**段階を書き忘れた PR の既定**

| 変更の中身 | 既定 |
|---|---|
| **文書だけ** | **段階2** |
| それ以外 | **段階1** |

**文書だけの PR を段階1に倒さない。** 段階1は「文書どうしの表現の食い違いを報告しない」と
定めているため、既定を段階1にすると、**この課題の成果物である文書が、
最も指摘を受けにくい設定になる。**

**段階1と段階2の仕分けは、依頼した側が行う。作業側は、その判定を PR の説明に写す。**
書くのは作業側だが、**決めるのは依頼側である。**
レビュアーには判定の経緯が見えないため、**PR の説明に書かれた段階を、そのまま宣言として読む。**

#### コメントと文書の表現は、段階2でのみ報告する

**段階1の PR に対して、次を報告しない。**

- 「なぜそうしたかが書かれていない」
- コメントの表現・言い回し
- 文書どうしの**表現**の食い違い（言い回し・語調）

**理由。** 段階1のあいだ、実装側は理由をコミットメッセージに書き、ソースには書かない
（調べ切る前に説明を書くと、誤った理由が残るため）。
**報告すると、実装側はその場で書くことになり、2段階に分けた意味が消える。**

**ただし次の3つは段階1でも報告する。** どれも動作に影響する。

| 報告する | なぜ |
|---|---|
| **コメントの内容が、コードの実際の動作と食い違っている** | 誤った理由付けは、次に触る人を誤らせる |
| **踏むと壊れる条件が書かれていない** | 次に触る人が壊す |
| **規則・手順を定めた文書どうしが食い違い、どちらを読むかで結果が変わる** | **成果物が手順である場合、これは表現ではなく動作の食い違いである** |

**3つめは「表現の食い違い」と区別する。** 言い回しが違うだけなら段階2。
**従う手順そのものが変わるなら段階1である。**

**段階2でも、上限で絞ったときの件数と見出しを必ず付ける**（上記「重大度の定義」の書き方に従う）。
実装側は**巡回ごとの総数が減っているか**で続行を判断する。
これが無いと、上限に当たった 5 件と、本当に 5 件しかない状態を区別できない。
**区別できないと、最も残っているときに「減っていない」と判断して止めてしまう。**

#### コメントと文書に、位置に依存した参照を書かない

**コメントは、書いた時点で「主張」になる。** コードと突き合わせられ、
食い違えば指摘の対象になる。**その主張が編集で黙ってずれる種類のものだと、
気づかないまま古くなり、レビューだけがそれを見つけ続ける。**

| 書かない | 代わりに |
|---|---|
| **行番号**（`〜は 118 行目を参照`） | **名前で指す**（関数名・変数名・見出し） |
| **相対の位置**（`前の項目`／`後述`／`上の3番目`） | **名前で指す。** 指す先に名前が無いなら、名前を付ける |
| **件数**（`確認は 53 通り`） | **実数から導く**か、機械が照合できる形にする |

**いずれも、ずれても動作は壊れない。** そのため誰も気づかない。

**相対の位置がいちばん壊れやすい。** 間に項目を1つ挿入するだけで、
**書き換えていない文の意味が変わる。** 書いた本人も気づかない。

実測。PR #29 の最後の巡回で出た 🟡4件のうち **3件がこれだった。**

| 書いてあったもの | 何が起きたか |
|---|---|
| 「その前提として」 | **間に項目が挿入され、別のものを指すようになった** |
| 「（後述）」 | 参照先が別ファイルへ移り、**同じ文書の中で解決しなくなった** |
| 「分かれ目はコンテナの有無ではない」 | **29行下の分岐を直した結果、上の説明と食い違った** |

**3件とも、直前の巡回で別の指摘を直したことによって生じている。**
位置に依存した参照がある限り、**1件直すたびに別の参照がずれる。**

**行番号と相対の位置は、コメントと文書の両方が対象である。**
上の実測3件は、いずれも文書（README・tech-stack）で起きている。

**件数は、機械が照合しているものだけが規則の対象外である。**

| 件数 | 照合の有無 |
|---|---|
| 機能の件数（「全 N 件」「合計 N 件」と区分の内訳） | **[check-docs.sh](scripts/check-docs.sh) が照合する。** 規則の対象外 |
| 「必ずテストを書く箇所」の N 項目 | **照合する。** 規則の対象外 |
| 検査の検査の「N 通り」 | **[check-docs.test.sh](scripts/check-docs.test.sh) が照合する。** 規則の対象外 |
| 認可の「N 経路」（実体は 2.1 の表の行数） | **[check-docs.sh](scripts/check-docs.sh) の検査5 が照合する。** 規則の対象外 |
| WebSocket イベントの「N 種類」（実体はイベント表） | **[check-docs.sh](scripts/check-docs.sh) の検査5 が照合する。** 規則の対象外 |

**「N 通り」は、`README.md` と `CLAUDE.md` に手で書いてあり、実数は
`scripts/check-docs.test.sh` が実行時に数える。**
**その2つを突き合わせる照合を、同じスクリプトが持っている。**
ケースを1つ足して文書を直し忘れると `docs` が落ちる。読み取れなくなった場合も落ちる。

以前ここに「文書の件数は機械が照合しており、実測で一致していた」と書いていたが、
**その実測は、当時まだ照合されていなかったほうの数（N 通り）を見ていた。**
一致していたのは機械が守っていたからではなく、**手で合わせ続けられていたからである。**
照合は PR #39 で足した（当初は #61 に分ける予定だったが、
`scripts/check-docs.test.sh` 側で完結するため同じ PR で入れた）。

**上の表に挙げた宣言は、どれも消してはならない。** 照合は「読み取れない」も NG にするため、
規則違反と読んで消すか言い回しを変えると `docs` が落ちる。
「N 経路」と「N 種類」も同じで、**書かれている場所すべてが照合の対象である**
（実数は [check-docs.sh](scripts/check-docs.sh) の検査5 が持つ。ここには写さない）。

**照合が依存している見出しと書式も同じ扱いである。** 検査5 は 2.1 の見出しと表の行の形、
イベント表の見出し、経路を列挙している行の書式を手がかりに読む。
そこを変えると数ではなく「読み取れない」で落ちる。

**書くなら、機械が照合できる形にする。** 照合できないなら書かない。

**代償。** 行番号や「前の」のほうが短く書ける。名前が重複していると一意に決まらず、
指す先に名前が無ければ**名前を付けるところから**になる。その場合は
**名前を付けるか、指すのをやめてその場に書く。** 位置での参照に戻さない。

**この規則で防げるのは「ずれること」だけである。** 事実そのものが誤っている、
あるいは前提が変わったコメント（例: 依存の既定の挙動が変わった）は対象外であり、
そちらは事情が変わるたびに直すしかない。

**この規則が縛るのは、リポジトリに残るものだけである。** レビューの指摘は従来どおり
`ファイル:行` で場所を示す（「報告の形式」）。**指摘はその場で読まれて消えるため、
ずれる前に役目を終える。** 残るものと残らないものを同じ規則で縛らない。

### このプロジェクト固有の設計観点

- **エラーの返し方**: 存在を隠すべきものは 404、権限不足は 403。**混同していないか**
- **削除**: メッセージは論理削除。**物理削除していないか**
- **判断の代償**: 方針を変える変更で、**その帰結が文書に書かれているか**（[CLAUDE.md](CLAUDE.md) 4）

---

## 5. 性能

このアプリで実際に問題になるものに絞る。**推測による性能指摘はしない。**

| 観点 | 確認すること |
|---|---|
| **N+1** | メッセージ一覧で、各メッセージのリアクション数・返信件数に `COUNT` を発行していないか。**カウンタ列を使っているか** |
| **ページネーション** | `OFFSET` を使っていないか。**カーソル方式か** |
| **索引** | 新しい検索条件・並び順を追加したのに、索引が無いまま放置していないか |
| **未読の集計** | 都度集計していないか。**既読位置からの差分で求めているか** |
| **WebSocket の配信量** | `typing` などの高頻度イベントを間引いているか |
| **フロントの再描画** | メッセージ一覧全体が毎回再描画されていないか。仮想化が効いているか |

---

## 6. テスト

| 観点 | 確認すること |
|---|---|
| **存在** | 実装に対応するテストが同じ変更に含まれているか |
| **有効性** | **そのテストは、実装が壊れたときに落ちるか**（通るだけのテストになっていないか） |
| **必須のテスト** | 下記の8項目に該当する変更で、テストが無い場合は 🔴 とする |

**本プロジェクトはテスト駆動開発で進める**（[要件定義書](docs/requirements.md) 4.8）。
テストが実装より後に書かれた形跡があっても、**それ自体は指摘しない**（履歴からは判定できない）。
見るのは上記の「有効性」— **そのテストが壊れたときに落ちるか**である。

### テストが必須の箇所

これらは**漏れやすく、漏れたときの影響が大きい**。

1. 検索がプライベートチャンネルを漏らさないこと
2. 添付ファイルの配信が非参加者を拒否すること
3. WebSocket が非参加者にイベントを配信しないこと
4. **WebSocket が許可外の Origin からのハンドシェイクを拒否すること**
5. Markdown が HTML として解釈されないこと（XSS）
6. 自分以外のメッセージを編集・削除できないこと
7. メンバーがオーナー専用の操作を実行できないこと
8. **オーナーが、参加していないプライベートチャンネルのメッセージを取得できないこと**

---

## 7. 指摘しないこと

**以下を報告すると、重要な指摘が埋もれる。**

| 対象 | 理由 |
|---|---|
| フォーマット・インデント・引用符 | **Prettier が判定する** |
| lint ルール違反 | **ESLint が判定する** |
| 型エラー | **tsc が判定する** |
| テストの成否 | **CI が判定する** |
| 命名の好み（`data` か `items` か等、どちらでも意味が通る場合） | 好みの問題 |
| 「将来こうなるかもしれない」への備えの提案 | **Google の指針に反する。今解くべき問題を解く** |
| ドキュメント・設定ファイルの表現の揺れ | **段階1に限る。** 動作に影響しないため。**段階2では報告する**（4章「コメントと文書の表現は、段階2でのみ報告する」）。この行を無条件に適用すると、段階2のレビューが何も報告できず、総数の比較が成立しなくなる |
| 生成されたファイル（Prisma のクライアント等） | 人が書いたものではない |
| **良い実装を褒めるコメント** | **修正する側にとってノイズになる。** 人間のレビューでは相手を傷つけないための緩衝材として意味があるが、**AI へのレビュー依頼にその配慮は要らない** |

---

## 8. 報告の形式

- **重大度を付ける**（🔴 / 🟡 / 🟣）
- **🔴 → 🟡 → 🟣 の順に書く。** 重大度を付けるだけでは並び順が決まらず、
  **読む側が並べ替えることになる。**
  🟡 は1件ずつが長い。**🔴 がその後ろに来ると、読み進めるまで気づけない。**
  これが「埋もれる」の実体である
- **1件ごとに `ファイル:行` を示す。** 場所の無い指摘は採用できない
- **「なぜ問題か」を書く。** 「◯◯すべき」だけでは判断できない
- **推測で書かない。** コードを読んで確認できたことだけを述べる。
  確認できていない場合は「未確認」と明記する
- **観点ごとの「問題なし」は書かない。** 指摘が1件も無い場合にかぎり、
  **レビュー全体として「指摘なし」と1行だけ述べる**（見ていないのと区別するため）。
  観点ごとに「見て問題なし」を列挙すると、その分量に 🔴 が埋もれる。
  これは1章で ⚪ を報告しないことにしたのと同じ理由による

---

## 出典

本ファイルの観点は、以下の公開されている実務基準に基づく。

| 基準 | 発行元 | 本ファイルでの利用 |
|---|---|---|
| [Code Review Developer Guide — What to look for in a code review](https://google.github.io/eng-practices/review/reviewer/looking-for.html) | Google | 4章（設計・機能・複雑さ・命名・コメント・一貫性・ドキュメント）。「スタイルは Nit として扱い、マージを止めない」「将来の問題への備えを推奨しない」という方針もここに拠る |
| [CWE Top 25 Most Dangerous Software Weaknesses](https://cwe.mitre.org/top25/) | MITRE | 3章の優先順位。2025年版では **XSS が1位、SQL インジェクションが2位、CSRF が3位**（39,080件の CVE を集計） |
| [OWASP Application Security Verification Standard (ASVS) 5.0](https://owasp.org/www-project-application-security-verification-standard/) | OWASP | 3章のセキュリティ観点。5.0（2025年5月）は17章・約350要件で構成される |
| [安全なウェブサイトの作り方 改訂第7版](https://www.ipa.go.jp/security/vuln/websecurity/index.html) | IPA（情報処理推進機構） | 3章。国内の実務で最も広く参照される。**「保険的対策は根本原因を除去しないため、それのみに依存する設計は推奨しない」**という指針を、本プロジェクトの XSS 対策（サニタイズではなく HTML を生成しない構造）に反映している |

### 出典を選んだ理由

- **Google の指針**は、コードレビューの実務基準として最も広く引用されており、
  「何を見るか」と「何を見ないか」の両方を明示している点で、AI への指示に適している
- **CWE Top 25** は実際の CVE を集計した実績データであり、**推測ではなく発生頻度に基づく優先順位**を与える
- **OWASP ASVS** は検証可能な要件の形をとっており、「安全にする」という抽象論ではなく
  チェック可能な項目に落ちている
- **IPA の資料**は国内の開発現場で共通言語として使われており、日本語で参照できる
