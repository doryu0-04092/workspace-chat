# コードレビュー観点

AI によるコードレビューに与える指示。人間のレビューでも同じ観点を使う。

**このファイルは「レビューが何を見るか」を定義する。** [CLAUDE.md](CLAUDE.md) はプロジェクトの
運用ルールを定義するもので、役割が違う。両方が読まれる前提で、重複を避けて書く。

---

## 0. このプロジェクトでのレビューの位置づけ

**AI レビューは指摘するだけであり、承認の責任は持たない。** マージの可否は人間が判断する。
「AI が OK と言ったからマージ」は成立しない。

**確認は3者で分担する。この分担が、レビューの範囲そのものである。**
**ただし「AI レビュー」が見る対象の実体は 9.1 の許可列挙（A〜E）であり、この表では数え直さない**——
数え直した版が 9.1 から漏れる。**項目を足すときは 9.1 に足す。**

| 誰が見るか | 何を見るか |
|---|---|
| **CI** | 書式を固定した宣言の照合。相対リンク・機能 ID の連番・件数・N経路・N種類（[check-docs.sh](scripts/check-docs.sh)）、lint・型・ビルド・テスト、依存の脆弱性。**壊れれば `docs` / `code` / `audit` が落ちる。レビューは重ねて見ない** |
| **AI レビュー** | **意味の比較と依存の追跡**——**機械が判定できないもの。** 見る対象は **9.1 の許可列挙（A〜E）がすべてである**。重大度は 1章、判定は 9章による |
| **誰も見ない** | 表現・語調・言い回し・**命名の好み**・**必要以上の複雑さ**・相対の位置（「後述」「直上」）・行番号・**固定書式の宣言に一致しない手書きの件数（**例外は 9.1 の E の台帳の行1つだけ**）** |

**機械が確実に判定できることを AI に投げない。** 非効率であり、指摘もぶれる。
**逆に、意味の比較と依存の追跡は機械にできない。** ここがレビューの担当範囲である。

**何を報告してよいか、どこで直すのを止めるかは 9章「判定規則」にある。**

## 1. 重大度の定義

| 記号 | 重大度 | 意味 | 扱い |
|---|---|---|---|
| 🔴 | **直さないと壊れる** | **実装が壊れる**（写した実装が穴を持つ、検査が偽の緑を出す、**必要なテストが無い**、結果が変わる）、**外的要因に触れる**（**9.1 の B のすべて**）、または**上流に決めてある値・規則・方式を外している**（**決めが無いものは対象外**。9.1 の一貫性の軸・9.2 の「一貫性（A）」「品質属性（C）」）。判定の具体は 9章 | 必ず報告する。**実装側は直す** |
| 🟡 | **記録する** | 9章の基準に当たるが、**🔴 のどれにも当たらない**——実装は壊れず、外的要因に触れず、**上流に決めが無い**（**下記の包括規則が 🔴 とする欠落を除く**。診断の名指しの質、記述と実態のずれ、重複、表題、必須8項目以外のテスト不足（**実装を読んで誤りが分かるものを除く**。6章）） | 報告する。**実装側は直さず、イシューに記録して節目で片づける**。**例外は 4章の設計の提案①②（揺れ・重複）だけである**（下記の包括規則） |
| 🟣 | **既存の問題** | この変更が原因ではないが、周辺に見つけた問題 | 報告する。**認可・秘密に関わるものは 🔴 と同じ扱い**（この PR で直す）。それ以外は 🟡 と同じ |
| ⚪ | **基準の外** | 表現・語調・言い回し・**命名の好み**・**必要以上の複雑さ**・相対の位置・行番号・**固定書式の宣言に一致しない件数**（**例外は 9.1 の E の台帳の行1つだけ**） | **報告しない** |

**認可・秘密に関わる 🟣 は、この文書と [CLAUDE.md](CLAUDE.md) の以降のすべての記述で「🔴」と読む。**
上限・順序・巡回の形・打ち切り・イシューに落とすかの判定は、**すべて 🔴 の側に従う。**
**この章の包括規則は、箇所ごとに書き足す形にはしない**（下記の2つにも同じことが当たる）。
足し漏れた箇所がそのまま穴になるためである
（実測: 3巡続けて足し漏れが 🔴 として出た。上限の側を塞いだ巡回で順序の側が開いた）。
**以降で 🟣 に触れている箇所は、この規則の言い換えであって、適用先の全部ではない。**
**認可・秘密でない 🟣 は、同じ規則で「🟡」と読む。**

**4章の設計の提案①②（揺れ・重複）は、この文書と [CLAUDE.md](CLAUDE.md) の以降のすべての記述で、
「その巡回で直すもの」の側に従う。** 上限の数え方・見出しのみに落とすか・イシューに落とすかの判定は、
**すべて 🔴 の側に従う**——上限の数に入れず、見出しのみに落とさず、イシューに落とさない。
**重大度と報告の順序は 🟡 のままである**（採るか戻すかを決めるのは依頼側であり、
作業側がするのは「手戻りできる形で直して報告する」ところまでだからである。4章）。
**以降で ①② に触れている箇所は、この規則の言い換えであって、適用先の全部ではない。**

**9.2 の型の表が 🔴 と定めたものは、この章の 🟡 に落ちない。**
上の3本の柱は**違反・食い違いの形**で書かれているが、**9.2 は欠落の側にも 🔴 を定めている**——
品質属性（C）の「決めるべきものが書かれていない」（可用性のバックアップ・復旧手順・アラート）と、
決定の到達（D）の「実装者が開くファイルに決定が書かれていない」がそれである。
**欠落であるか食い違いであるかを問わず、9.2 が 🔴 とするものは 🔴 である。**
**この文書の他の箇所にある列挙は、9.2 の言い換えであって、適用先の全部ではない。**

**⚪ は報告しない。** 好みの指摘が混ざると、🔴 が埋もれて読まれなくなる。

**🔴 に上限は無い。見つけた分をすべて書く。**
上限がかかるのは 🟡 だけである。ここを書き分けないと、🔴 にも上限があると誤読される。

**1回のレビューで、詳細まで書く 🟡 は最大5件までとする。**
上限は、詳細が多すぎて **🔴 が埋もれるのを防ぐため**である
（🔴 と、認可・秘密に関わる 🟣 は必ず先頭に書く。「報告の形式」に順序を定めている）。

**上限は 🟡 だけを数える。🔴 と、認可・秘密に関わる 🟣 は数に入れない。**
**この2つは、その PR で直す対象である**（1章の 🟣 の行・9.4）。数に入れると、🟡 で枠が埋まったとき
**実装側が直すべきものが上限の外へ押し出され**、
下の「上限を超えた分は見出しだけ」に落ちて、**その巡回では直されない。**
判定の具体は 9章「判定規則」にある。

**上限を超えた分は、件数だけでなく見出しを1行ずつ書く。**

```
他に 2 件（見出しのみ）:
  6. compose.yaml のコメントが、存在しない depends_on を根拠にしている
  7. README の「（後述）」が、この文書の中で解決しない
```

**件数だけでは、次の巡回に持ち越せない。**
レビューは巡回ごとの記憶を持たないため、**超えた分が何だったかを誰も知らないまま消える。**
次の巡回で同じものが再び見つかる保証も無い（実測では、前後の巡回で指摘が重複しなかった）。

見出しがあれば、実装側は次の3つができる。

1. **超えた分をイシューに落とす**（記録が残る）
2. **次の巡回で、その分を優先して直させる**
3. **総数の比較に加えて、同じ指摘が残っているのか入れ替わったのかを区別する**

**見出しだけの分は、その巡回では直さない。**
このファイルの「報告の形式」が求める「なぜ問題か」「どう直すか」が
付いていないため、**実装側が判断できない。** イシューに落とし、次の巡回で詳細を受け取る。

**ワークフローの指示は、出力の作法についてこのファイルを参照するだけである。**
同じ事項をワークフロー側にも書くと、どちらを読むかで出力が変わる。
実際に「件数だけ伝える」という古い指示が残っていて食い違った（#62）。
**同じ規則を2箇所に書かない。**

---

## 2. 最優先で確認すること（このプロジェクト固有）

**以下は実装漏れが起きやすく、漏れたときの影響が最も大きい。** ここを最初に見る。

### 2.1 認可 — プライベートチャンネルは4経路すべてを塞ぐ

プライベートチャンネルの内容が非参加者に渡る経路は4つある。**1つでも塞ぎ忘れると要求が成立しない。**

| # | 経路 | 確認すること |
|---|---|---|
| 1 | **一覧・取得 API** | 非参加者に 404 を返すか。403 ではないか（存在を隠す）。**応答の本体も見る**——404 を返していても、本文が「権限がありません」であれば存在を認めている。**本文は「見つかりません」で揃っているか** |
| 2 | **検索** | 検索クエリが「自分が参加しているチャンネルと DM」に限定されているか |
| 3 | **添付ファイルの配信** | **署名付き Cookie が、参加者に対してそのチャンネルのパス配下に限って発行されているか。** 対象パスが広すぎないか（`/*` になっていないか）。**配信用の S3 のキーが `workspace/{ws}/channel/{ch}/...` の構造から外れていないか**（キー設計が認可の一部である）。**Cookie の対象は配信 URL のパス（`/files/` を前置したもの）であり、S3 のキーではない**——**配信 URL のパス・署名付き Cookie の対象・その `Path` 属性・CloudFront のビヘイビアの4つが `/files` で揃い、S3 のキーだけがそれを持たないか**（**`Path` を省くと既定値が発行時の URI のディレクトリ（`/api/...`）になり、`/files/...` に一度も送られない。`Path=/` にすると対象の範囲と実際に送られる範囲が食い違う**。[要件定義書](docs/requirements.md) 4.3）。**そして、添付のオリジンへ `/files/*` 以外のビヘイビアから到達できないか**（例外はアバターの `/avatars/*` である。**オリジンへは正規化の前のパスが渡るため、ドットセグメントや符号化したドットを含む URL でも `avatars/` の外のキーに届かないことを確かめるテストか手順があるか**（[要件定義書](docs/requirements.md) 4.3。未確認の前提である）。その Cookie の対象が `/avatars/*` を超えていないか）——**`/files` を剥がしたパスは S3 のキーと同じ形であり、署名を要求しない既定のビヘイビアが同じオリジンを指していると、`/files` を外した URL で署名を通らずに取得できる**。**アップロード用の署名付き URL の発行が、参加者判定を持つか。署名するキーが、すべてサーバー側で組み立てられているか**（`{ws}/{ch}` は判定を通したチャンネル、`{UUID}` はサーバーの発行、`{ファイル名}` は許可リストでの置換。クライアントが指定したキーをそのまま署名していないか）（アップロードは配信とは別の経路であり、Cookie とは別の関門である）。**署名付き URL の宛先が隔離用のキー（`quarantine/workspace/{ws}/channel/{ch}/...`）であり、有効期限が5分であり、確定の段で参加者判定をやり直し、検証した版に固定して配信用のキーへコピーしているか。確定の要求がクライアントの渡したキー・チャンネルでコピー先を決めていないか（サーバーが払い出した識別子に紐づく値だけで組み立てているか）**（隔離用のキーは配信されないキーであり、配信用のキーの構造の外にあってよい）。**アップロード URL の署名に、`quarantine/` にだけ書ける専用の資格情報を使い、確定（隔離用のキーの読み出し・削除と、配信用のキーへのコピー）の資格情報と分けているか**（同じプリンシパルだと、ブラウザからの PUT も配信用のキーへ通る）。**確定を求められるのが、識別子を払い出された本人だけか**。**同じ識別子の確定を1回だけ行うか**。**確定の要求にレート制限があるか**。**確定で参加者判定に落ちたときに 3.1 の2段階のコードを返し、本人以外・別チャンネルの識別子に 404 を返すか**。**署名付き URL が `If-None-Match: *` を署名に含むか**。**ライフサイクルのフィルターが `quarantine/` だけに掛かり、配信用のキーに掛からないか** |
| 4 | **WebSocket の配信** | 非参加者の接続にイベントが配信されていないか。**ハンドシェイクで `Origin` を検証しているか**（CORS は WebSocket に効かない） |

**特に 2 と 3 は漏れやすい。** 画面に出さない実装をしていても、API が返していれば漏洩である。

**加えて、オーナーの閲覧範囲を確認する。** オーナーは参加していないプライベートチャンネルの
**`id` / 名前 / 種別 / 参加者数と参加者一覧は取得できるが、メッセージ・添付ファイル・未読数は取得できない**
（[機能一覧](docs/features.md) 3.1「オーナーには、管理のためのチャンネル一覧を返す」）。
この境界を越えていないか。**例外が及ぶのは一覧・取得 API だけである。**

### 2.2 認可はサーバー側にあるか

- **フロントエンドでボタンを隠すだけになっていないか**
- 「自分のメッセージのみ編集・削除できる」がサーバー側で検証されているか
- 「オーナーのみが招待・キック・チャンネル操作を行える」がサーバー側で検証されているか
- **クライアントから送られた `userId` や `role` を信用していないか**（トークンから導出すべき）

### 2.3 要件の3分類と顧客要望

- **承認を得ていない機能が実装されていないか**（[CLAUDE.md](CLAUDE.md) 1）
- [機能一覧](docs/features.md) に無い機能が追加されていないか
- 追加されている場合、区分と根拠が記載されているか

**顧客要望を満たしているか。** このプロジェクトの顧客要望は**提出要件**であり、その実体は
[機能一覧](docs/features.md) の一覧表「根拠・承認」列に引用された**課題文**である
（「新規登録(ユーザーIDとパスワードのみ)」「メッセージ検索」など）。
変更が触る機能 ID について、**課題文が求めているものが受け入れ条件に落ちているか**を見る。

**これは技術的な問題ではなく、要求管理の問題である。**

### 2.4 WebSocket のイベント型が二重定義されていないか

フロントエンドとバックエンドで、イベント名や引数の型を別々に書いていないか。
**共有の型定義から参照しているか。**

### 2.5 XSS — Markdown が HTML として解釈されないか

- `dangerouslySetInnerHTML` が使われていないか
- サニタイズの設定が外されていないか
- **`javascript:` `data:` などのスキームのリンクが無効化されているか**

---

## 3. セキュリティ

判断基準は **CWE Top 25**、**OWASP ASVS 5.0**、**IPA「安全なウェブサイトの作り方」** に準拠する
（出典は末尾）。以下はこのプロジェクトで実際に起こりうるものに絞った。

| 観点 | 確認すること | 参照 |
|---|---|---|
| **XSS** | 利用者入力が HTML として解釈されないか。Markdown 描画、表示名、ステータス、チャンネル名すべて | CWE-79（2025年 **1位**） |
| **SQL インジェクション** | `$queryRaw` で文字列を連結していないか。**特に検索の絞り込み演算子（`from:` `in:`）と pg_bigm のクエリ** | CWE-89（**2位**） |
| **CSRF** | Cookie を使うエンドポイント（リフレッシュ・ログアウト）に対策があるか | CWE-352（**3位**） |
| **アクセス制御の不備** | 2.1 / 2.2 のとおり | CWE-284 / CWE-639 |
| **リソースの無制限確保** | アップロード、WebSocket イベント、メッセージ投稿にレート制限があるか | CWE-770 |
| **パストラバーサル** | ファイル名を利用者入力からそのまま組み立てていないか | CWE-22 |
| **WebSocket の Origin 検証** | **CSWSH 対策があるか。WebSocket は同一オリジンポリシーの対象外である** | — |
| **認証情報の露出** | パスワード・トークンがレスポンス、ログ、エラーメッセージに出ていないか | — |
| **エラーメッセージ** | 認証失敗で「ID が無い」と「パスワードが違う」を区別していないか | — |
| **アップロード検証** | 中身から形式を検証しているか（マジックバイトを持たないテキスト系は UTF-8 として正しく NUL を含まないこと）。**拒否リストではなく許可リストか**。**配信の `Content-Type` をサーバーが決め（アップロード時の値を引き継がない）、`nosniff` を返し、保存名の拡張子を検証した形式に付け替え、`Content-Disposition` を画像・動画は `inline`・それ以外は `attachment` にしているか**。**アバター画像も同じ段（隔離→検証→確定）を通り、サーバーが multipart で受けていないか。アバターのキーが `avatars/{uid}/`（隔離用は `quarantine/avatars/{uid}/`）で始まり、`{uid}` が認証した本人の `User.id` か。`avatarUrl` にサーバーが組み立てた配信 URL のパスだけを入れているか** | — |
| **識別子の推測** | ファイルID・メッセージID が連番になっていないか | — |
| **依存の脆弱性** | 新しい依存を追加する PR で、その必要性が説明されているか | — |

---

## 4. 設計と可読性

判断基準は **Google の Code Review Developer Guide** に準拠する（出典は末尾）。

Google が挙げる観点のうち、**このプロジェクトで AI に見てほしいもの**を選んだ。
スタイルは CI（lint / formatter）が判定するため、レビューでは扱わない。
**命名の好みと「複雑さ」も外した**——どちらも 0章の「誰も見ない」に挙げてある。
報告すると 🔴 が埋もれる（7章）。

**「機能」（意図どおりに動くか）も外した**——動くかは 6章のテストの経路で見る。
テストが担保するのは表明したことだけであり、**レビューの仕事は「表明が足りているか」を見ることに移した。**

**Google の「Design」は4問ある。この表は、4問すべてを別の形で受けている。**

| Google の問い | どこで受けるか |
|---|---|
| 各部分のやり取りが筋が通っているか | **結果が変わるもの**（検証・認可より前の保存、途中失敗での矛盾）は 9.1 の A。**良くできるもの**（責任の分担・呼び出しの契約）は 4章「設計」 |
| この変更はここに属するか | 決めがあれば「**一貫性**」、無ければ 4章「**設計**」の提案 |
| 既存と統合されるか | 同上 |
| いま足す時期か | [CLAUDE.md](CLAUDE.md) 1——**承認を得ていない機能を実装しない**。時期の判断を、承認の有無という事実に置き換えている |

| 観点 | 確認すること |
|---|---|
| **一貫性** | **決めてあるやり方があれば、それに従っているか**（命名の形式・エラーの返し方・識別子・削除の方式・決めた状態の後の扱い・置き場）。**決めが無いものは見ない——書き方には自由がある** |
| **設計** | 責任の分担・呼び出しの契約・置き場・重複・決めの無い状態の遷移について、**より良い形の提案**。**🔴 にはならない**——ただし**下記「設計の提案の条件」の除外文に当たるものは、そもそも提案ではない**。条件・効果の数え方・作業側の扱いもそこにある（決めがあるものは「一貫性」または「品質属性（C）」で見る） |
| **コメント** | **コメントの内容が、コードの実際の動作と食い違っていないか。** 特に「消してよい」「この形で書け」「これを足すな」と**行動を指示する**コメントと、実装者が写す**参照実装**の記述。**表現・言い回しは見ない** |
| **踏むと壊れる条件** | 依存されている側（設定・定義・データ）に、**壊す変更をすると何が落ちるかの注記が「無い」**こと。**注記の文言の過不足・向き・前提の失効は見ない** |
| **ドキュメント** | 挙動が変わったのに [機能一覧](docs/features.md) や [要件定義書](docs/requirements.md) が古いままになっていないか。**同じ条件・キー・認可境界・値が2つの文書で違っていないか** |

### 設計の提案の条件

提案は3種類あり、**種類によって作業側の扱いが違う。**

**この表の①②③のいずれにも、9.2 が 🔴 と定めるものは入らない。**
B（外的要因）、9.2 の C が 🔴 と定めるもの（**上流に決めてある値・規則を外している**——
性能の目標値・**イベント型の一元化**——／**決めるべきものが書かれていない**）、
9.2 の「一貫性（A）」が 🔴 と定めるもの、9.2 の「決定の到達（D）」が 🔴 と定めるものは、
**提案ではなく 🔴 である。提案は「決めが無いところ」にだけ立つ。**
**重大度・件数の上限・下記の足切りは、提案として扱うと決まってから掛かる**——
9.2 の 🔴 をここへ入れると、**上限と足切りによって提案としてすら出なくなる。**
**箇所ごとに書き足す形にはしない。** 足し漏れた経路がそのまま穴になる
（実測: B → C の欠落 → C の違反 → ②の重複、と4巡にわたって1つずつ出た）。

| # | 種類 | 条件 | 効果の数え方 | 作業側の扱い |
|---|---|---|---|---|
| ① | **揺れ**（ネガティブ） | 上流に決めを1つ足せば揺れが消える | 揃う箇所の数 | **できる範囲で直し、手戻りできる形にして報告する**（[CLAUDE.md](CLAUDE.md)「手戻りできる形にする」）。採るか戻すかは依頼側 |
| ② | **重複**（ネガティブ） | 2箇所以上で同じことをしている | 消える重複の数 | 同上 |
| ③-量 | **改良・量**（ポジティブ） | 承認済みの F-ID が**2つ以上**、その形で実装が楽になる共通化 | 恩恵を受ける F-ID の数 | **提案のみ。直さない。** 採ると決まってから作業になる |
| ③-質 | **改良・質**（ポジティブ） | 9.1 の C の属性を**上げるために足すもの**（**決めが無いところへの上積み**。上記の除外文）。**性能は根拠（計測か目標値）が要る**（5章） | 上がる属性と、効く経路・機能の数 | 同上 |

**共通の制限。**

- 1箇所の書き方をこう変えたほうが良い、は提案しない（0章の「誰も見ない」）
- **文書の表現・体裁は含めない。新しい機能は含めない**（[CLAUDE.md](CLAUDE.md) 1 の経路で扱う）。**「将来こうなるかもしれない」への備えは含めない**（7章）
- **効果と変更量（ファイル数）を併記する。** 大きいかを決めるのはレビュアーではなく依頼側であり、この2つの数がその材料になる。効果が変更量を上回らないものは提案しない
- **③は量・質それぞれ1巡回に1件まで**——無くても壊れないため、最も効果の大きいものだけを出す
- ①②は「変更に比して改善が大きい」形そのものである——1つの決めで複数箇所が揃う／複数箇所が1つに寄る

**③を直さないのは、提案を「壊れている」前提で見なくて済むようにするためである。**
①②は直さないと揺れ・重複が残るが、③は待って損がない。先に直すと差分が増え、次巡に「足した説明」として乗る。

### このプロジェクト固有の設計観点

- **エラーの返し方**: 存在を隠すべきものは 404、権限不足は 403。**混同していないか。応答の本体まで揃っているか**（404 の本文が「権限がありません」だと、存在を隠す目的が果たされない）
- **削除**: メッセージは論理削除。**物理削除していないか**

---

## 5. 性能

このアプリで実際に問題になるものに絞る。**推測による性能指摘はしない。**

| 観点 | 確認すること |
|---|---|
| **N+1** | メッセージ一覧で、各メッセージのリアクション数・返信件数に `COUNT` を発行していないか。**カウンタ列を使っているか** |
| **ページネーション** | `OFFSET` を使っていないか。**カーソル方式か** |
| **索引** | 新しい検索条件・並び順を追加したのに、索引が無いまま放置していないか |
| **未読の集計** | 都度集計していないか。**既読位置からの差分で求めているか** |
| **WebSocket の配信量** | `typing` などの高頻度イベントを間引いているか |
| **フロントの再描画** | メッセージ一覧全体が毎回再描画されていないか。仮想化が効いているか |

---

## 6. テスト

| 観点 | 確認すること |
|---|---|
| **存在** | 実装に対応するテストが同じ変更に含まれているか |
| **有効性** | **そのテストは、実装が壊れたときに落ちるか**（通るだけのテストになっていないか） |
| **必須のテスト** | 下記の8項目に該当する変更で、テストが無い場合は 🔴 とする。**8項目に当たらなくても、実装を読んで誤りが分かるものは 🔴**（下記） |

**本プロジェクトはテスト駆動開発で進める**（[要件定義書](docs/requirements.md) 4.8）。
テストが実装より後に書かれた形跡があっても、**それ自体は指摘しない**（履歴からは判定できない）。
見るのは上記の「有効性」— **そのテストが壊れたときに落ちるか**である。

**「有効性」を見る視点は1つに固定する**——**「このテストは、守るべき性質が壊れたときに落ちるか」。**
テスト自身の内部の頑健さ（診断の名指しの質、壊す確認そのものが壊れたら落ちるか）は**見ない**。
そこまで広げると、壊す確認を足すたびに「その壊す確認に落ちる条件が無い」という次の指摘が生まれ、
巡回が自己生成する（実測: 1つの PR で7巡）。

**🟡 とするテスト不足**（下記の2つに当たらないもの）は、その場では直さず、テスト追加のイシューに集約し、
**節目**（機能 ID の実装が一区切りついたとき／実装フェーズに入る前／提出前）でまとめて追加する。
**「無い」と「効いていない」を分ける。**
テストが**無い**（不足）が 🔴 になるのは**2つ**である——**必須8項目に当たる**とき、
または**実装を読んで誤りが分かる**とき（下記）。**それ以外の不足は 🟡** とする。
**テストがあるのに効いていない（偽の緑）のは、8項目によらず 🔴 である**（1章・9.1 の A のとおり）。
**偽の緑は「テストがあるのに、壊れても落ちない」状態であり、CI は緑のまま守るべき性質が無防備になる。**
**イシューに集約するのは「不足」だけである。偽の緑は集約しない。**

**「実装を読んで誤りが分かる」の側を、ここで定める。**
**テストが無く、かつ実装を読んで誤りが分かるものは 🔴** とし、その巡回で直す——
ページングの境界、同時実行の競合、同じ型どうしの引数の取り違えがこれに当たる。

**この区別が要るのは、CI がこの組み合わせだけを止められないためである。**
テストが無いのだから CI は緑で通り、実装の誤りはそのまま残る。
**🟡 として節目へ送ると、誤ったまま次の機能が積まれる。**

**見るのは、テストを書く段階と、検査の段階である。**

| 何 | テストを書く段階で決めること | 検査の段階で確かめること |
|---|---|---|
| **ページングの境界** | 最初のページ・最後のページ・件数ちょうどの境界を1文で書き、**カーソル方式の境界条件として表明する**（5章） | 実際にその境界を踏み、**期待したページ（境界をまたがない集合）が返ること**を確かめる。「たまたま境界をまたがなかった」では通らない形にする |
| **同時実行の競合** | 「同時に実行されたとき何が成り立つべきか」を1文で書き、**その不変条件をデータストアの制約に置く**（一意制約・条件付き挿入・行ロック） | 同時に実行して、**制約違反が起きること**か**1件だけ成功すること**を確かめる。「たまたま順序が良かった」では通らない形にする |
| **同じ型どうしの引数の取り違え** | **識別子を素の値のまま渡さない。** 型で区別するか、**位置引数をやめて名前で渡す** | 取り違えたら落ちる値を使う（すべて別の値にする）。**型で区別できていれば、確かめるのは型検査器である** |

**3つとも、手当ての本筋は「レビューで指摘する」ではなく「構造で起きなくする」である。**
**ページングの境界**の構造の手当ては、9.1 の C（上流に決めてある値・規則——カーソル方式。5章）で見る。
**同時実行の競合・同じ型どうしの引数の取り違え**の構造の手当てが無いことは、9.1 の A
（データ整合がデータストア側で担保されている／参照実装を写しても穴が無い）で見る。

### テストが必須の箇所

これらは**漏れやすく、漏れたときの影響が大きい**。

1. 検索がプライベートチャンネルを漏らさないこと
2. 添付ファイルの配信とアップロード用の署名付き URL の発行が、非参加者を拒否すること
3. WebSocket が非参加者にイベントを配信しないこと
4. **WebSocket が許可外の Origin からのハンドシェイクを拒否すること**
5. Markdown が HTML として解釈されないこと（XSS）
6. 自分以外のメッセージを編集・削除できないこと
7. メンバーがオーナー専用の操作を実行できないこと
8. **オーナーが、参加していないプライベートチャンネルのメッセージ・添付ファイル・未読数を取得できないこと**

---

## 7. 指摘しないこと

**報告してよいのは、9章「判定規則」の基準に当たるものだけである。**
**列挙されていないものは報告しない。** 禁止されていないから報告してよい、とは読まない。
これは、レビューが巡回ごとの記憶を持たないためである——禁止の列挙で運用すると、
**禁止されていない新しい指摘が毎回見つかり、巡回が終わらない**（実測: 242巡回で 🟡 661件、
そのうち実装が壊れるものは 38%）。

以下は特に、報告すると重要な指摘が埋もれる。

| 対象 | 誰が見るか |
|---|---|
| フォーマット・インデント・引用符 | **Prettier**（`code`） |
| lint ルール違反 | **ESLint**（`code`） |
| 型エラー | **tsc**（`code`） |
| テストの成否 | **Vitest**（`code`） |
| 依存の脆弱性の有無 | **npm audit**（`audit`）。**レビューが見るのは「新しい依存の必要性が説明されているか」だけ**（3章） |
| 相対リンクの妥当性・機能 ID の連番・件数（「合計 N 件」「N 通り」「N 経路」「N 種類」） | **[check-docs.sh](scripts/check-docs.sh)**（`docs`）。**壊れれば落ちる。レビューは重ねて見ない** |
| **位置に依存した参照**（行番号・「前の」「後述」「直上」）と、**固定書式の宣言に一致しない手書きの件数（**例外は 9.1 の E の台帳の行1つだけ**）** | **誰も見ない。** ずれても動作は壊れない（実測: この型の指摘 65件のうち、実装が壊れるものは 0件）。**固定書式の宣言に書かれた件数だけは [check-docs.sh](scripts/check-docs.sh) が照合する**（上の行） |
| 表現・語調・言い回しの揺れ | **誰も見ない** |
| 命名の好み（`data` か `items` か等、どちらでも意味が通る場合） | **誰も見ない** |
| 「将来こうなるかもしれない」への備えの提案 | **Google の指針に反する。今解くべき問題を解く** |
| 生成されたファイル（Prisma のクライアント等） | 人が書いたものではない |
| **良い実装を褒めるコメント** | **修正する側にとってノイズになる。** 人間のレビューでは相手を傷つけないための緩衝材として意味があるが、**AI へのレビュー依頼にその配慮は要らない** |

---

## 8. 報告の形式

- **重大度を付ける**（🔴 / 🟡 / 🟣）
- **🔴 と、認可・秘密に関わる 🟣 → 🟡 → それ以外の 🟣 の順に書く。**
  重大度を付けるだけでは並び順が決まらず、
  **読む側が並べ替えることになる。**
  🟡 は1件ずつが長い。**🔴 がその後ろに来ると、読み進めるまで気づけない。**
  これが「埋もれる」の実体である
- **1件ごとに `ファイル:行` を示す。** 場所の無い指摘は採用できない
- **「なぜ問題か」を書く。** 「◯◯すべき」だけでは判断できない
- **推測で書かない。** コードを読んで確認できたことだけを述べる。
  確認できていない場合は「未確認」と明記する
- **観点ごとの「問題なし」は書かない。** 指摘が1件も無い場合にかぎり、
  **レビュー全体として「指摘なし」と1行だけ述べる**（見ていないのと区別するため）。
  観点ごとに「見て問題なし」を列挙すると、その分量に 🔴 が埋もれる。
  これは1章で ⚪ を報告しないことにしたのと同じ理由による
- **冒頭に、対象の機能 ID を書く。** PR の説明の `機能 ID:` 行を読む。
  **行が無い場合**は差分から推定して「（推定）」と明記し、E の 🟡 として1件報告する。
  **「なし」と書かれている場合は、推定も報告もしない**（9.4）
- **1件ごとに、当たった基準の記号を付ける**（A〜E と細目。例: `A 参照実装` / `B 認可4経路`）。
  あわせて、**その指摘が守らせようとしている規則を1行で書く**（例: 「準備の失敗は名指しして止める」）。
  記号だけでは、実装側が台帳の束と照合できない（9.4「同じ束」の判定）
  依頼側はこの記号で仕分けるため、**記号の無い指摘は判定できない**
- **規則の未適用は、適用先を列挙して1件として報告する。**
  1箇所見つけたら、同じ規則・決定・記述が当たる箇所をすべて挙げる。
  **1巡に1〜3箇所ずつ出さない**——実測で、1つの規則の適用漏れを1箇所ずつ報告したために
  7巡・9巡を要した例がある（9章「整合性の単位」）

---

## 9. 判定規則

**ここが唯一の判定規則である。** [CLAUDE.md](CLAUDE.md)「AI レビューの指摘に対応する」はこれを参照する。

> **なぜ `CLAUDE.md` ではなくここに置くか。** CI のレビュアーは `CLAUDE.md` を PR の版で読めない。
> ワークフローがルートの `CLAUDE.md` を既定ブランチの版に差し替え、PR の版を `.claude-pr/CLAUDE.md` へ
> 退避するためである（#46 で確認）。**`CLAUDE.md` に置くと、`CLAUDE.md` を変更する PR で、
> その PR 自身に適用できない。** このファイルは PR の版がそのまま読まれる。
> レビュアー自身が重大度と基準の記号を付ける以上、判定規則はレビュアーが読む文書に無ければならない。

### 9.1 報告してよいもの（許可列挙。列挙外は報告しない）

| 群 | 軸 | 実体 |
|---|---|---|
| **A 内的要因** | 矛盾が無い（**起点は、触った機能 ID の節とその参照先。機能 ID が無い PR では、差分の触ったファイルと、そこに書かれた規則の名前を起点とする**。そこから下流をたどって適用先の全体を見る——9.3。全文書を端から読むことはしない） | 4章「ドキュメント」 |
| | **削除が論理削除である**（メッセージを物理削除していないか） | 4章「このプロジェクト固有の設計観点」 |
| | 実装可能である（書けないものを「書け」と指示していないか） | 9.2「実行不能な指示（A）」 |
| | 参照実装を写しても穴が無い | 4章「コメント」 |
| | 検査・テストが、**守るべき性質が壊れたときに落ちる**（**偽の緑**と、**必要なテストが無いこと**の両方。**視点は1つ**。6章） | 6章「存在」「有効性」 |
| | データ整合が DB 側で担保されている（アプリの検証1箇所の漏れで壊れないか） | [CLAUDE.md](CLAUDE.md) 2 |
| | エラー時に失敗が名指しされ、**途中で失敗しても保存済みのものが矛盾したまま残らない**（握り潰していないか。トランザクション境界） | 3章「エラーメッセージ」 |
| | 依存・版の整合（[技術スタック](docs/tech-stack.md) と実際） | [技術スタック](docs/tech-stack.md) |
| | 環境差（手元・CI・デプロイ先でずれが緑のまま隠れないか） | `eslint.config.js` / `.prettierignore` / `scripts/lint-scope.test.sh` の注記（「手元でだけ落ちる」の実例と、それを機械で確かめる検査） |
| | データの流れと責任の所在（**検証・認可より前に保存・配信していないか。認可の判定がサーバーにあるか**） | 2.2 |
| | **決めてあるやり方に従っている**（命名の形式・エラーの返し方・識別子・削除の方式・決めた状態の後の扱い・置き場）。**決めが無いものは見ない** | 4章「一貫性」 |
| | **設計の提案**（責任の分担・呼び出しの契約・置き場・重複・決めの無い状態の遷移）。**🟡 のみ**（**9.2 が 🔴 と定めるものは提案に入らない**。4章の除外文）。**①②（揺れ・重複）は作業側が直し、採るか戻すかは依頼側**（4章） | 4章「設計」 |
| **B 外的要因** | セキュリティ12観点 | 3章 |
| | 認可の4経路 ＋ オーナーの閲覧範囲 | 2.1 |
| | 秘密の露出（レスポンス・ログ・コミット） | 3章 / [CLAUDE.md](CLAUDE.md) 禁止事項 |
| | 法令（電気通信事業法・個人情報保護法・外部送信規律・**商標とトレードドレス**・絵文字ライセンス） | [要件定義書](docs/requirements.md) 6 |
| | **顧客要望**（このプロジェクトでは提出要件＝課題文） | 2.3 |
| | 要件の3分類（承認の有無・区分の記載） | 2.3 |
| **C 品質属性** | 性能 | 5章 |
| | 可観測性 | [要件定義書](docs/requirements.md) 4.6 |
| | 保守性（**4.7 が挙げる具体項目だけ**: イベント型の一元化・REST 仕様からの型生成・生成物の差分検証。一般語としての「保守性」は見ない） | [要件定義書](docs/requirements.md) 4.7 |
| | 可用性（**バックアップ・復旧手順・アラートが書かれているか**。フェイルオーバーは「有無が決まっていて、無いなら代償が書かれているか」だけを見る。**どちらを採るかは指定しない**） | [要件定義書](docs/requirements.md) 4.2 |
| **D 文書の性質** | 決定が、実装者が読む場所に届いているか（`schema.prisma` の docblock・参照実装） | 「実装者が読む場所」の定義（9.1） |
| | 踏むと壊れる条件の注記が**無い**こと | 4章 |
| **E プロセス** | 責任の所在（仕分け・打ち切り・マージの条件は依頼側。**レビューは申告のみ**） | 0章 |
| | PR の目的・機能 ID と差分が一致し、**目的外の変更を含まない** | 8章 |
| | **台帳の適用先の欄に、引いたコマンドと、直した後の2つの件数**（引き直したときのヒット数と、**そのうち規則を満たしていないものの件数**）**があるか。** 無ければ、または**後者が 0 でなければ**、列挙し切らないまま push している。**これは 1章 ⚪ の「手書きの件数」の例外である**——**見るのは欄の有無と、宣言された後者の値が 0 かだけであり、件数そのものが正しいかは照合しない**（照合できるのは `check-docs.sh` が持つ固定書式の宣言だけである。7章） | [CLAUDE.md](CLAUDE.md)「台帳の運用」4 |

**A〜E の実体は、この表がすべてである。** **この文書のどこであれ**、群の中身を挙げている箇所は
**言い換えであって、適用先の全部ではない。** 項目を足すときは**この表に足す**。
**列挙を各所に複製しない**——足し漏れた項目がそのまま穴になる
（実測: 列挙が複数箇所に複製された結果、同じ指摘が3巡・2巡と続けて閉じられなかった）。
**とくに B は、この表の6行すべてである**——ここでも数え直さない（数え直した版がこの表から漏れる）。

**「実装者が読む場所」とは、その作業において、変更・確認を問わず開くことが決まっているファイルである。**
判定は1問——**その決定に関わる作業をするとき、そのファイルを開かずに済むか。** 済むなら届いていない。
**[CLAUDE.md](CLAUDE.md) に書いただけでは届かない**——実装者は触るファイルを開くのであって、規則文書を毎回開くわけではない。
**検証はレビューの出力で行える**——レビュアーが判断のために開いたファイル（`REVIEW.md` / `CLAUDE.md` を除く）に
その決定が書かれていれば届いており、書かれていなければ届いていない。

### 9.2 🔴 と 🟡 の閾値

**🔴 は「直さないと実装が壊れる／外的要因に触れる」。型ごとの具体は次のとおり。**

**この表が決めるのは重大度だけである。報告してよいかは 9.1 が決める。**
表の行は 9.1 の複数の軸から到達しうる（例: 「動作」の行には、環境差と、エラー時・トランザクション境界から到達する）。
**9.1 の軸は、1行ずつこの表に到達できなければならない**——到達できないと、
「見る」と決めたものが必ず 🟡 に落ちる（実測: 見ると決めた品質属性の軸が表に到達しておらず、🟡 に落ちていたことがあった）。
**数える単位は群ではなく軸である。** 群として到達していても、到達しない軸があればそこが穴になる。
**確かめるのは到達だけではない。** **9.1 の軸 → この表の行 → 1章の重大度 → 9.4 の扱い**が
**1本につながっていること**を単位とする。**途中で切れると、そこで 🟡 に落ちる。**
（実測: 到達の脚だけを不変条件にしていたため、鎖の残りが5巡にわたって1本ずつ出た。）
**例外は2つある。** **B（外的要因）は表を経ない**——下記のとおり条件なしで 🔴 である。
**E（プロセス）は申告のみで 🟡 に固定する**
（0章「レビューは申告のみ」・9.4「機能 ID の行が無い PR の既定」）。

**判定は動線で行う。** この文書群は、上流から下流へ流れる。

```
課題文（顧客要望）
  → 要件定義書（決定・非機能）
    → 機能一覧（受け入れ条件・区分）
      → schema.prisma の docblock / 参照実装（実装者が最初に読み、写す）
        → 実装
CLAUDE.md・REVIEW.md（規則） → 作業のやり方
scripts のコメント → 次に触る人の変更
```

**外的要因——9.1 の B のすべて——は、条件なしで 🔴 である**（1章）。
**食い違いであるか欠落であるかを問わない。** 課題文が求めているものが受け入れ条件に落ちていない、
商標・絵文字ライセンスの要件を満たしていない——これらは**上流どうしの食い違いではなく欠落**であるが、
下の2条件を当てはめてはならない。**当てはめると 🟡 に落ち、イシューへ送られて節目まで直されない。**

**下の2条件は、外的要因に触れない「食い違い」の側にだけかかる。**
**その食い違いが 🔴 になるのは、次の2つを同時に満たすときである。**

> **(a) 上流の成果物に書かれており、かつ**
> **(b) その食い違いが、下流の選択を変える。**

**(a) だけでは足りない。** この repo はほぼ全部が上流であり、
「上流の食い違いは 🔴」とすると文書の食い違いがすべて 🔴 になって、範囲が元に戻る。

**(b) は次の1問で判定する。**

> **その記述に、そのまま従った結果が壊れているか。**

**従う対象があるもの**（指示・雛形・手順・参照実装）が実態と違えば 🔴。
**従う対象がないもの**（説明・診断の文言・経緯）のずれは 🟡 である。
「実装に関わる記述かどうか」で切らない。**それでは文書のほとんどが 🔴 になり、範囲が元に戻る。**

- 「件数は CI が引き受ける」と書いてあるが、CI が照合するのは固定書式の宣言だけ →
  従うと**照合されない件数を書いて通す** → 🔴
- NG のメッセージが誤った原因を名指ししている → **従う対象が無い**（読む人は原因を探す。検査は現に落ちる）→ 🟡
- 同じ代償が2つの節に全文で書かれている → **下流はどちらを読んでも同じものを作る** → 🟡

**この形は、適用先を探すのにも使う。** 上流で1箇所見つけたら、**下流をたどって**
その食い違いが届く先をすべて挙げる（9.3「整合性の単位」）。

| 型 | 🔴 になるもの | 🟡 になるもの |
|---|---|---|
| 認可・秘密 | **すべて。🟣 も含む**（1章のとおり 🔴 と同じ扱いで、その PR で直す） | — |
| テスト | **必須8項目**（6章）が**無い**／**テストが無く、実装を読んで誤りが分かる**（6章）／**偽の緑**（テストがあるのに、守るべき性質が壊れても落ちない。**8項目によらない**） | それ以外の**テスト不足**（無いこと） |
| 動作 | **結果が変わる**（緑と赤が反転する・戻り値が変わる・環境によって止まる） | **原因の名指しが誤る**（赤にはなる。診断の質） |
| コメント | **行動を指示する**もの（「消してよい」「この形で書け」「足すな」）と**参照実装**の記述が、実態と違う | 記述と実態のずれ（指示形ではないもの） |
| 文書間 | **条件・キー・認可境界・値**が2文書で違う | 重複・表題・番号の不一致 |
| 品質属性（C） | **上流に決めてある値・規則を外している**（性能の目標値、イベント型の一元化）、または**決めるべきものが書かれていない**（可用性のバックアップ・復旧手順・アラート） | **決めが無く、改善の余地を述べるもの**（「N+1 になりうる」「こう書いたほうが速い」） |
| 設計 | — | より良い形の**提案**（条件は 4章「設計の提案の条件」）。**①②（揺れ・重複）は作業側が直して報告し、③（改良）は提案のみで直さない。採るか戻すかは依頼側が決める** |
| 一貫性（A） | **上流に決めてある規則・方式を外している**（9.1 A「決めてあるやり方に従っている」の6項目のいずれかを外している） | — （**⚪。9.1 A・4章「一貫性」のとおり、決めが無いものは見ない**——決めの無い揺れは自由であり、より良い形は 4章の設計の提案で出す） |
| 決定の到達（D） | **実装者が開くファイル**（`schema.prisma` の docblock・参照実装）**に決定が書かれていない** | 規則文書には書かれており、届き先が**実装者の開かないファイル**に留まる |
| 実行不能な指示（A） | **従うことが原理的にできない**（満たせない終了条件、書けないものを「書け」）。規則文書・手順・参照実装のいずれでも | — |
| 踏むと壊れる注記 | 被参照側に注記が無く、**写すと壊れる**場合 | 診断の質に留まる場合 |

**判定例**

| 実例 | 判定 | 理由 |
|---|---|---|
| 参照実装の docblock が、要求者の識別子を「リクエスト由来」と書いている | **🔴** | 写した実装が、他人の識別子を受け取って認可を破る |
| `lower()` で照合するテストが、片側だけを固定していて改変で落ちない | **🔴** | 偽の緑。受け入れ条件が静かに落ちる |
| 失敗の名指しが誤っていて、原因から遠い案内が出る | 🟡 | 赤にはなる。誤るのは診断の指し先だけ |
| 同じ代償が2つの節に全文で書かれている | 🟡 | どちらも現時点では一致している。正典に当たれば戻れる |
| コメントが「N 行目」「直上」を指している | ⚪ | 報告しない（0章の「誰も見ない」） |

**「🔴 か 🟡 か」が割れるものは、レビュアーが自分の読みを述べたうえで「判定は依頼側」と明記する。**
**仕分けを確定するのは依頼側である**（E）。

### 9.3 整合性の単位と「壊れている」の判定単位

**整合性を取る単位は「同じ規則・決定・記述が当たる箇所の全体」。**
編集された近傍でも、全文書でもない。境界は規則が決める。

**「壊れている」の判定単位は「規則の適用状態」。**
1箇所でも欠けていれば、それは「その規則が未適用」であって「その1箇所が壊れている」ではない。
したがって**報告も修正も、適用先の全体を単位とする**（8章）。

**「1 PR 1目的」の「目的」には、「1つの規則・決定を、その適用先全体に当てること」を含む。**
複数のファイル・複数の機能に跨っても、規則が1つなら1目的である。
**適用先が PR の目的を超える場合は、規則単位の別の PR に分ける。**

**2つの文書が食い違ったら、動線の上流を正とし、下流を直す**（課題文 → 要件定義書 → 機能一覧 → 実装。9.2 の動線）。
**どちらが正か迷うなら、それは重複を残した代償である。1つに寄せ、もう片方は参照にする**（#9 の形）。

**ただし、動線の上下では決まらない食い違いがある。次の表が向きを定める。**

| 食い違ったもの | どちらが正か |
|---|---|
| **規則**（不変条件・手順・判定規則） | **規則文書**（この文書 / [CLAUDE.md](CLAUDE.md)）**が正。** 記録側を直す |
| **実測の数字と経緯** | **[docs/review-criteria-record.md](docs/review-criteria-record.md) が正。** 規則文書を直す |
| **規則文書どうし**（この文書 ⇄ [CLAUDE.md](CLAUDE.md)） | **持ち主で決める**（この文書の冒頭の役割分担）。**この文書の9章が持つものは、この文書が正**——基準の一覧（A〜E）・🔴 と 🟡 の閾値・整合性の単位・巡回の形・打ち切りの条件（[CLAUDE.md](CLAUDE.md) 自身が「ここには書かない」としてこの5つを名指しで手放している）。**それ以外の運用は [CLAUDE.md](CLAUDE.md) が正**——権限の範囲・開発フロー・記録の置き場・**作業側の**報告。**「手順」でも「報告」でも語で切らない**——レビュアーの出力の作法は 8章「報告の形式」であり、**この文書が持つ**（[claude_code_review.yml](.github/workflows/claude_code_review.yml) が現にそこを指しており、9章の冒頭が「レビュアーは `CLAUDE.md` を PR の版で読めない」と定めている）——巡回の手順は 9.4 と `CLAUDE.md` の両方にあり、語で切ると重なって向きが割れる。正でない側は参照1行に寄せる |

**上の「動線の上流を正とする」が当たるのは、同じ種類の中で食い違ったときである。**
**規則文書どうしには当たらない**——9.2 の動線は `CLAUDE.md・REVIEW.md（規則）` を**同じ節点**に置いており、
**上流が決まらない。** だから持ち主で決める。
**[CLAUDE.md](CLAUDE.md)「文書どうしが食い違ったら」は、この規則を参照するだけである**——
**向きを申告するのはレビュアーであり**（0章の分担表）、**レビュアーは `CLAUDE.md` を PR の版で読めない**（9章の冒頭・#46）。

### 9.4 巡回の形

1. **基準段階** — **🔴 と、認可・秘密に関わる 🟣 を直す。** それ以外の 🟣 と 🟡 は
   その巡回でイシューに記録する（テスト不足はテスト追加のイシューに集約）。
   **4章の設計の提案①②だけは、🟡 のままその巡回で直して報告する**（1章の例外）。
   **🔴 と、認可・秘密の 🟣 が出なくなったら**次へ。**巡回数の上限は置かない**（閾値で有限になる）
2. **仕上げ** — コメント・文章の修正を**1回だけ**入れる。「減るまで回す」はしない

**マージは巡回に含まれない。** 0章のとおり、可否を判断するのは人間である。
巡回が終わったら、**CI 4本（`code` / `docs` / `audit` / `review`）の完走を確認して報告する**
（`review` の `success` は「完走した」であって「指摘なし」ではない）。
**マージするかどうかは、開発フロー 0 で確認した権限の範囲による**（[CLAUDE.md](CLAUDE.md)）。

**打ち切りは、回数ではなく理由で決める。** 止めるのは次の2つのときだけである。

- **直す方法が無い**——規則そのものに反する、定義に反する、不変条件どうしが矛盾する（直せば別の何かが壊れる）
- **判断が人に属する**——設計判断が絡む（受け入れ条件・区分・認可境界・データモデル・非機能の数値が変わる）

**直す方法があるなら、何巡目でも直す。** 回数は判定の材料であって条件ではない。
ただし**同じ束が5巡以上になったら、要因を記録して依頼側へ報告する**（巡回は止めない。止めると実装が遅れる）。
回数が増えるのは、難しくなっているか、大きい原因が潜んでいるかの兆候である。

**「同じ束」は、規則の名前で判定する。** 台帳（PR 本文）の各束は規則の名前と不変条件を持つ。
新しい指摘が守らせようとしている規則が、台帳のどれかと一致すれば同じ束である。
**箇所が違っても、記述が新しくても、規則が同じなら同じ束として数える。**

| 見え方 | 実体 | 直し方 |
|---|---|---|
| 前巡と同じ箇所・同じ内容 | **直っていない** | 直し方が誤っている。設計を変える |
| 同じ規則が**別の箇所**で出た | **適用先の列挙が漏れた** | `grep` をやり直し、残りを一度に直す |
| **前巡で足した記述**が対象になった | **自己生成** | 足した記述を**消す方向**で直す |

**3つとも同じ束として数える。** 「新規かどうか」で数えると、
**適用先の漏れと自己生成が「新しい指摘」に見えて、5巡以上での要因の報告が発動しない**
（実測: 1つの規則の適用漏れが5巡、足した記述への指摘が2巡続いた）。

**台帳に、束ごとの出た巡回を記録する。** 同じ束が続くことは、上の2つの理由の兆候である。
数えるのは指摘の件数ではなく、**束が出た巡回の数**である。

**より良い方法に気づいたときは、それを理由に止めない。** できる範囲で直し、**手戻りできる形**にしたうえで
依頼側へ報告する（[CLAUDE.md](CLAUDE.md)「手戻りできる形にする」）。**採るかどうかは人が決める。**
これは提案であって作業報告ではない。**置き場は環境で決まる**——一人で開発しているならイシューにしてよく、
複数人ならローカルに一時的に置いて人の確認を得てからイシューにする（[CLAUDE.md](CLAUDE.md)「手戻りできる形にする」）。

**別の束の 🔴 は、何巡目であってもそのまま直す**（打ち切りの対象ではない）。

**イシューに落とすもの**: 🟡 のすべて（テスト不足はテスト追加のイシューへ集約。**4章の設計の提案①②を除く**——1章のとおり作業側が直す）、
上限を超えて見出しだけで返った分のうち 🟡 のもの。
**🔴 と、認可・秘密に関わる 🟣 は落とさない**（直すか、規則単位の別 PR に分ける）。

**機能 ID の行が無い PR の既定**: 差分から推定して報告の冒頭に「（推定）」と明記し、
E の 🟡 として1件報告する。
**「機能 ID: なし」と書かれている PR**: 推定も 🟡 の報告も**しない**。宣言されている以上、
書かれていない場合には当たらない。

**A〜E は、どの PR でも**すべて**当てる。範囲を宣言で絞らない。**
`機能 ID: なし` の PR で機能要件（受け入れ条件を満たすか、区分が書かれているか、性能の目標値を外していないか）の
指摘が出ないのは、**見ないからではなく、当たる対象が無いから**である。
**「見ない」と書くと、見に行かなくなる。** 見に行って何も無ければ、何も出ない——それが正しい形である。
**B の実体は 9.1 の表の B の行すべてである**——
ここで数え直さない（数え直した版が 9.1 から漏れる）。
**群の中身を列挙で書かない**のは、足し漏れた項目がそのまま穴になるためである（9.1 末尾・1章の包括規則と同じ理由）。
1章の「🟣 でも認可・秘密に関わるものは 🔴 と同じ扱い」が無条件であることとも対応する。

**9.2 の「外的要因は条件なしで 🔴」は、重大度の話であって報告範囲の話ではない。**
**何を見るか**はこの節が PR ごとに決め、**見たものをどの重大度にするか**を 9.2 が決める。

**記録**: 巡回数をこのファイルに書かない。**実数は、そのブランチのレビューのワークフロー実行履歴から数える。**
**射程は進行中の巡回に限る。過去の実測は、根拠として引いてよい**（削除しない。章を限定せず、本文中に既にある実測すべてを指す）。

---

## 出典

本ファイルの観点は、以下の公開されている実務基準に基づく。

| 基準 | 発行元 | 本ファイルでの利用 |
|---|---|---|
| [Code Review Developer Guide — What to look for in a code review](https://google.github.io/eng-practices/review/reviewer/looking-for.html) | Google | 4章（一貫性・設計・コメント・踏むと壊れる条件・ドキュメント）。**複雑さと命名は、このプロジェクトでは 0章の「誰も見ない」に置いた**（Google は Nit として扱うが、記憶を持たないレビュアーでは毎回新しく見つかるため外した）。「将来の問題への備えを推奨しない」という方針はここに拠る |
| [CWE Top 25 Most Dangerous Software Weaknesses](https://cwe.mitre.org/top25/) | MITRE | 3章の優先順位。2025年版では **XSS が1位、SQL インジェクションが2位、CSRF が3位**（39,080件の CVE を集計） |
| [OWASP Application Security Verification Standard (ASVS) 5.0](https://owasp.org/www-project-application-security-verification-standard/) | OWASP | 3章のセキュリティ観点。5.0（2025年5月）は17章・約350要件で構成される |
| [安全なウェブサイトの作り方 改訂第7版](https://www.ipa.go.jp/security/vuln/websecurity/index.html) | IPA（情報処理推進機構） | 3章。国内の実務で最も広く参照される。**「保険的対策は根本原因を除去しないため、それのみに依存する設計は推奨しない」**という指針を、本プロジェクトの XSS 対策（サニタイズではなく HTML を生成しない構造）に反映している |

### 出典を選んだ理由

- **Google の指針**は、コードレビューの実務基準として最も広く引用されており、
  「何を見るか」と「何を見ないか」の両方を明示している点で、AI への指示に適している
- **CWE Top 25** は実際の CVE を集計した実績データであり、**推測ではなく発生頻度に基づく優先順位**を与える
- **OWASP ASVS** は検証可能な要件の形をとっており、「安全にする」という抽象論ではなく
  チェック可能な項目に落ちている
- **IPA の資料**は国内の開発現場で共通言語として使われており、日本語で参照できる
