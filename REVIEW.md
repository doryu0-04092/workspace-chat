# コードレビュー観点

AI によるコードレビューに与える指示。人間のレビューでも同じ観点を使う。

**このファイルは「レビューが何を見るか」を定義する。** [CLAUDE.md](CLAUDE.md) はプロジェクトの
運用ルールを定義するもので、役割が違う。両方が読まれる前提で、重複を避けて書く。

---

## 0. このプロジェクトでのレビューの位置づけ

**AI レビューは指摘するだけであり、承認の責任は持たない。** マージの可否は人間が判断する。
「AI が OK と言ったからマージ」は成立しない。

**確認は3者で分担する。この分担が、レビューの範囲そのものである。**

| 誰が見るか | 何を見るか |
|---|---|
| **CI** | 書式を固定した宣言の照合。相対リンク・機能 ID の連番・件数・N経路・N種類（[check-docs.sh](scripts/check-docs.sh)）、lint・型・ビルド・テスト、依存の脆弱性。**壊れれば `docs` / `code` / `audit` が落ちる。レビューは重ねて見ない** |
| **AI レビュー** | **意味の比較**（2文書で条件・キー・認可境界・値が違わないか、指示形のコメントが実態と合っているか、参照実装を写しても穴が無いか）、**依存の追跡**（踏むと壊れる条件、認可の4経路の網羅）、**判断の申告**（重大度と、判断が要るものの提示） |
| **誰も見ない** | 表現・語調・言い回し・**命名の好み**・**必要以上の複雑さ**・相対の位置（「後述」「直上」）・行番号・**固定書式の宣言に一致しない手書きの件数** |

**機械が確実に判定できることを AI に投げない。** 非効率であり、指摘もぶれる。
**逆に、意味の比較と依存の追跡は機械にできない。** ここがレビューの担当範囲である。

**何を報告してよいか、どこで直すのを止めるかは 9章「判定規則」にある。**

## 1. 重大度の定義

| 記号 | 重大度 | 意味 | 扱い |
|---|---|---|---|
| 🔴 | **直さないと壊れる** | **実装が壊れる**（写した実装が穴を持つ、検査が偽の緑を出す、結果が変わる）、または**外的要因に触れる**（認可・秘密・法令・顧客要望）。判定の具体は 9章 | 必ず報告する。**実装側は直す** |
| 🟡 | **記録する** | 9章の基準に当たるが、**実装は壊れない**（診断の名指しの質、記述と実態のずれ、重複、表題、必須8項目以外のテスト不足） | 報告する。**実装側は直さず、イシューに記録して節目で片づける** |
| 🟣 | **既存の問題** | この変更が原因ではないが、周辺に見つけた問題 | 報告する。**認可・秘密に関わるものは 🔴 と同じ扱い**（この PR で直す）。それ以外は 🟡 と同じ |
| ⚪ | **基準の外** | 表現・語調・言い回し・**命名の好み**・**必要以上の複雑さ**・相対の位置・行番号・**固定書式の宣言に一致しない件数** | **報告しない** |

**⚪ は報告しない。** 好みの指摘が混ざると、🔴 が埋もれて読まれなくなる。

**🔴 に上限は無い。見つけた分をすべて書く。**
上限がかかるのは 🟡 だけである。ここを書き分けないと、🔴 にも上限があると誤読される。

**1回のレビューで、詳細まで書く 🟡 は最大5件までとする。**
上限は、詳細が多すぎて **🔴 が埋もれるのを防ぐため**である
（🔴 は必ず先頭に書く。「報告の形式」に順序を定めている）。

**上限は 🟡 だけを数える。🔴 と、認可・秘密に関わる 🟣 は数に入れない。**
**この2つは、その PR で直す対象である**（1章の 🟣 の行・9.4）。数に入れると、🟡 で枠が埋まったとき
**実装側が直すべきものが上限の外へ押し出され**、
下の「上限を超えた分は見出しだけ」に落ちて、**その巡回では直されない。**
判定の具体は 9章「判定規則」にある。

**上限を超えた分は、件数だけでなく見出しを1行ずつ書く。**

```
他に 2 件（見出しのみ）:
  6. compose.yaml のコメントが、存在しない depends_on を根拠にしている
  7. README の「（後述）」が、この文書の中で解決しない
```

**件数だけでは、次の巡回に持ち越せない。**
レビューは巡回ごとの記憶を持たないため、**超えた分が何だったかを誰も知らないまま消える。**
次の巡回で同じものが再び見つかる保証も無い（実測では、前後の巡回で指摘が重複しなかった）。

見出しがあれば、実装側は次の3つができる。

1. **超えた分をイシューに落とす**（記録が残る）
2. **次の巡回で、その分を優先して直させる**
3. **総数の比較に加えて、同じ指摘が残っているのか入れ替わったのかを区別する**

**見出しだけの分は、その巡回では直さない。**
このファイルの「報告の形式」が求める「なぜ問題か」「どう直すか」が
付いていないため、**実装側が判断できない。** イシューに落とし、次の巡回で詳細を受け取る。

**ワークフローの指示は、出力の作法についてこのファイルを参照するだけである。**
同じ事項をワークフロー側にも書くと、どちらを読むかで出力が変わる。
実際に「件数だけ伝える」という古い指示が残っていて食い違った（#62）。
**同じ規則を2箇所に書かない。**

---

## 2. 最優先で確認すること（このプロジェクト固有）

**以下は実装漏れが起きやすく、漏れたときの影響が最も大きい。** ここを最初に見る。

### 2.1 認可 — プライベートチャンネルは4経路すべてを塞ぐ

プライベートチャンネルの内容が非参加者に渡る経路は4つある。**1つでも塞ぎ忘れると要求が成立しない。**

| # | 経路 | 確認すること |
|---|---|---|
| 1 | **一覧・取得 API** | 非参加者に 404 を返すか。403 ではないか（存在を隠す） |
| 2 | **検索** | 検索クエリが「自分が参加しているチャンネルと DM」に限定されているか |
| 3 | **添付ファイルの配信** | **署名付き Cookie が、参加者に対してそのチャンネルのパス配下に限って発行されているか。** 対象パスが広すぎないか（`/*` になっていないか）。**S3 のキーが `workspace/{ws}/channel/{ch}/...` の構造から外れていないか**（キー設計が認可の一部である） |
| 4 | **WebSocket の配信** | 非参加者の接続にイベントが配信されていないか。**ハンドシェイクで `Origin` を検証しているか**（CORS は WebSocket に効かない） |

**特に 2 と 3 は漏れやすい。** 画面に出さない実装をしていても、API が返していれば漏洩である。

**加えて、オーナーの閲覧範囲を確認する。** オーナーは参加していないプライベートチャンネルの
**`id` / 名前 / 種別 / 参加者数と参加者一覧は取得できるが、メッセージ・添付ファイル・未読数は取得できない**
（[機能一覧](docs/features.md) 3.1「オーナーには、管理のためのチャンネル一覧を返す」）。
この境界を越えていないか。**例外が及ぶのは一覧・取得 API だけである。**

### 2.2 認可はサーバー側にあるか

- **フロントエンドでボタンを隠すだけになっていないか**
- 「自分のメッセージのみ編集・削除できる」がサーバー側で検証されているか
- 「オーナーのみが招待・キック・チャンネル操作を行える」がサーバー側で検証されているか
- **クライアントから送られた `userId` や `role` を信用していないか**（トークンから導出すべき）

### 2.3 要件の3分類と顧客要望

- **承認を得ていない機能が実装されていないか**（[CLAUDE.md](CLAUDE.md) 1）
- [機能一覧](docs/features.md) に無い機能が追加されていないか
- 追加されている場合、区分と根拠が記載されているか

**顧客要望を満たしているか。** このプロジェクトの顧客要望は**提出要件**であり、その実体は
[機能一覧](docs/features.md) の一覧表「根拠・承認」列に引用された**課題文**である
（「新規登録(ユーザーIDとパスワードのみ)」「メッセージ検索」など）。
変更が触る機能 ID について、**課題文が求めているものが受け入れ条件に落ちているか**を見る。

**これは技術的な問題ではなく、要求管理の問題である。**

### 2.4 WebSocket のイベント型が二重定義されていないか

フロントエンドとバックエンドで、イベント名や引数の型を別々に書いていないか。
**共有の型定義から参照しているか。**

### 2.5 XSS — Markdown が HTML として解釈されないか

- `dangerouslySetInnerHTML` が使われていないか
- サニタイズの設定が外されていないか
- **`javascript:` `data:` などのスキームのリンクが無効化されているか**

---

## 3. セキュリティ

判断基準は **CWE Top 25**、**OWASP ASVS 5.0**、**IPA「安全なウェブサイトの作り方」** に準拠する
（出典は末尾）。以下はこのプロジェクトで実際に起こりうるものに絞った。

| 観点 | 確認すること | 参照 |
|---|---|---|
| **XSS** | 利用者入力が HTML として解釈されないか。Markdown 描画、表示名、ステータス、チャンネル名すべて | CWE-79（2025年 **1位**） |
| **SQL インジェクション** | `$queryRaw` で文字列を連結していないか。**特に検索の絞り込み演算子（`from:` `in:`）と pg_bigm のクエリ** | CWE-89（**2位**） |
| **CSRF** | Cookie を使うエンドポイント（リフレッシュ・ログアウト）に対策があるか | CWE-352（**3位**） |
| **アクセス制御の不備** | 2.1 / 2.2 のとおり | CWE-284 / CWE-639 |
| **リソースの無制限確保** | アップロード、WebSocket イベント、メッセージ投稿にレート制限があるか | CWE-770 |
| **パストラバーサル** | ファイル名を利用者入力からそのまま組み立てていないか | CWE-22 |
| **WebSocket の Origin 検証** | **CSWSH 対策があるか。WebSocket は同一オリジンポリシーの対象外である** | — |
| **認証情報の露出** | パスワード・トークンがレスポンス、ログ、エラーメッセージに出ていないか | — |
| **エラーメッセージ** | 認証失敗で「ID が無い」と「パスワードが違う」を区別していないか | — |
| **アップロード検証** | マジックバイトを検証しているか。**拒否リストではなく許可リストか** | — |
| **識別子の推測** | ファイルID・メッセージID が連番になっていないか | — |
| **依存の脆弱性** | 新しい依存を追加する PR で、その必要性が説明されているか | — |

---

## 4. 設計と可読性

判断基準は **Google の Code Review Developer Guide** に準拠する（出典は末尾）。

Google が挙げる観点のうち、**このプロジェクトで AI に見てほしいもの**を選んだ。
スタイルは CI（lint / formatter）が判定するため、レビューでは扱わない。
**命名の好みと「複雑さ」も外した**——どちらも 0章の「誰も見ない」に挙げてある。
報告すると 🔴 が埋もれる（7章）。

| 観点 | 確認すること |
|---|---|
| **設計** | その変更はここに属するか。既存の仕組みと整合しているか |
| **機能** | 意図したとおりに動くか。**エッジケース、並行処理の問題を見る** |
| **一貫性** | 既存のコードのやり方から不必要に外れていないか |
| **コメント** | **コメントの内容が、コードの実際の動作と食い違っていないか。** 特に「消してよい」「この形で書け」「これを足すな」と**行動を指示する**コメントと、実装者が写す**参照実装**の記述。**表現・言い回しは見ない** |
| **踏むと壊れる条件** | 依存されている側（設定・定義・データ）に、**壊す変更をすると何が落ちるかの注記が「無い」**こと。**注記の文言の過不足・向き・前提の失効は見ない** |
| **ドキュメント** | 挙動が変わったのに [機能一覧](docs/features.md) や [要件定義書](docs/requirements.md) が古いままになっていないか。**同じ条件・キー・認可境界・値が2つの文書で違っていないか** |

### このプロジェクト固有の設計観点

- **エラーの返し方**: 存在を隠すべきものは 404、権限不足は 403。**混同していないか**
- **削除**: メッセージは論理削除。**物理削除していないか**

---

## 5. 性能

このアプリで実際に問題になるものに絞る。**推測による性能指摘はしない。**

| 観点 | 確認すること |
|---|---|
| **N+1** | メッセージ一覧で、各メッセージのリアクション数・返信件数に `COUNT` を発行していないか。**カウンタ列を使っているか** |
| **ページネーション** | `OFFSET` を使っていないか。**カーソル方式か** |
| **索引** | 新しい検索条件・並び順を追加したのに、索引が無いまま放置していないか |
| **未読の集計** | 都度集計していないか。**既読位置からの差分で求めているか** |
| **WebSocket の配信量** | `typing` などの高頻度イベントを間引いているか |
| **フロントの再描画** | メッセージ一覧全体が毎回再描画されていないか。仮想化が効いているか |

---

## 6. テスト

| 観点 | 確認すること |
|---|---|
| **存在** | 実装に対応するテストが同じ変更に含まれているか |
| **有効性** | **そのテストは、実装が壊れたときに落ちるか**（通るだけのテストになっていないか） |
| **必須のテスト** | 下記の8項目に該当する変更で、テストが無い場合は 🔴 とする |

**本プロジェクトはテスト駆動開発で進める**（[要件定義書](docs/requirements.md) 4.8）。
テストが実装より後に書かれた形跡があっても、**それ自体は指摘しない**（履歴からは判定できない）。
見るのは上記の「有効性」— **そのテストが壊れたときに落ちるか**である。

**「有効性」を見る視点は1つに固定する**——**「このテストは、守るべき性質が壊れたときに落ちるか」。**
テスト自身の内部の頑健さ（診断の名指しの質、壊す確認そのものが壊れたら落ちるか）は**見ない**。
そこまで広げると、壊す確認を足すたびに「その壊す確認に落ちる条件が無い」という次の指摘が生まれ、
巡回が自己生成する（実測: 1つの PR で7巡）。

**必須8項目以外のテスト不足は 🟡 とする。** その場では直さず、テスト追加のイシューに集約し、
**節目**（機能 ID の実装が一区切りついたとき／実装フェーズに入る前／提出前）でまとめて追加する。
必須8項目に当たるものだけが 🔴 である（外的要因）。

### テストが必須の箇所

これらは**漏れやすく、漏れたときの影響が大きい**。

1. 検索がプライベートチャンネルを漏らさないこと
2. 添付ファイルの配信が非参加者を拒否すること
3. WebSocket が非参加者にイベントを配信しないこと
4. **WebSocket が許可外の Origin からのハンドシェイクを拒否すること**
5. Markdown が HTML として解釈されないこと（XSS）
6. 自分以外のメッセージを編集・削除できないこと
7. メンバーがオーナー専用の操作を実行できないこと
8. **オーナーが、参加していないプライベートチャンネルのメッセージを取得できないこと**

---

## 7. 指摘しないこと

**報告してよいのは、9章「判定規則」の基準に当たるものだけである。**
**列挙されていないものは報告しない。** 禁止されていないから報告してよい、とは読まない。
これは、レビューが巡回ごとの記憶を持たないためである——禁止の列挙で運用すると、
**禁止されていない新しい指摘が毎回見つかり、巡回が終わらない**（実測: 242巡回で 🟡 661件、
そのうち実装が壊れるものは 38%）。

以下は特に、報告すると重要な指摘が埋もれる。

| 対象 | 誰が見るか |
|---|---|
| フォーマット・インデント・引用符 | **Prettier**（`code`） |
| lint ルール違反 | **ESLint**（`code`） |
| 型エラー | **tsc**（`code`） |
| テストの成否 | **Vitest**（`code`） |
| 依存の脆弱性の有無 | **npm audit**（`audit`）。**レビューが見るのは「新しい依存の必要性が説明されているか」だけ**（3章） |
| 相対リンクの妥当性・機能 ID の連番・件数（「合計 N 件」「N 通り」「N 経路」「N 種類」） | **[check-docs.sh](scripts/check-docs.sh)**（`docs`）。**壊れれば落ちる。レビューは重ねて見ない** |
| **位置に依存した参照**（行番号・「前の」「後述」「直上」）と、**固定書式の宣言に一致しない手書きの件数** | **誰も見ない。** ずれても動作は壊れない（実測: この型の指摘 65件のうち、実装が壊れるものは 0件）。**固定書式の宣言に書かれた件数だけは [check-docs.sh](scripts/check-docs.sh) が照合する**（上の行） |
| 表現・語調・言い回しの揺れ | **誰も見ない** |
| 命名の好み（`data` か `items` か等、どちらでも意味が通る場合） | **誰も見ない** |
| 「将来こうなるかもしれない」への備えの提案 | **Google の指針に反する。今解くべき問題を解く** |
| 生成されたファイル（Prisma のクライアント等） | 人が書いたものではない |
| **良い実装を褒めるコメント** | **修正する側にとってノイズになる。** 人間のレビューでは相手を傷つけないための緩衝材として意味があるが、**AI へのレビュー依頼にその配慮は要らない** |

---

## 8. 報告の形式

- **重大度を付ける**（🔴 / 🟡 / 🟣）
- **🔴 → 🟡 → 🟣 の順に書く。** 重大度を付けるだけでは並び順が決まらず、
  **読む側が並べ替えることになる。**
  🟡 は1件ずつが長い。**🔴 がその後ろに来ると、読み進めるまで気づけない。**
  これが「埋もれる」の実体である
- **1件ごとに `ファイル:行` を示す。** 場所の無い指摘は採用できない
- **「なぜ問題か」を書く。** 「◯◯すべき」だけでは判断できない
- **推測で書かない。** コードを読んで確認できたことだけを述べる。
  確認できていない場合は「未確認」と明記する
- **観点ごとの「問題なし」は書かない。** 指摘が1件も無い場合にかぎり、
  **レビュー全体として「指摘なし」と1行だけ述べる**（見ていないのと区別するため）。
  観点ごとに「見て問題なし」を列挙すると、その分量に 🔴 が埋もれる。
  これは1章で ⚪ を報告しないことにしたのと同じ理由による
- **冒頭に、対象の機能 ID を書く。** PR の説明の `機能 ID:` 行を読む。
  **行が無い場合**は差分から推定して「（推定）」と明記し、E の 🟡 として1件報告する。
  **「なし」と書かれている場合は、推定も報告もしない**（9.4）
- **1件ごとに、当たった基準の記号を付ける**（A〜E と細目。例: `A 参照実装` / `B 認可4経路`）。
  あわせて、**その指摘が守らせようとしている規則を1行で書く**（例: 「準備の失敗は名指しして止める」）。
  記号だけでは、実装側が台帳の束と照合できない（9.4「同じ束」の判定）
  依頼側はこの記号で仕分けるため、**記号の無い指摘は判定できない**
- **規則の未適用は、適用先を列挙して1件として報告する。**
  1箇所見つけたら、同じ規則・決定・記述が当たる箇所をすべて挙げる。
  **1巡に1〜3箇所ずつ出さない**——実測で、1つの規則の適用漏れを1箇所ずつ報告したために
  7巡・9巡を要した例がある（9章「整合性の単位」）

---

## 9. 判定規則

**ここが唯一の判定規則である。** [CLAUDE.md](CLAUDE.md)「AI レビューの指摘に対応する」はこれを参照する。

> **なぜ `CLAUDE.md` ではなくここに置くか。** CI のレビュアーは `CLAUDE.md` を PR の版で読めない。
> ワークフローがルートの `CLAUDE.md` を既定ブランチの版に差し替え、PR の版を `.claude-pr/CLAUDE.md` へ
> 退避するためである（#46 で確認）。**`CLAUDE.md` に置くと、`CLAUDE.md` を変更する PR で、
> その PR 自身に適用できない。** このファイルは PR の版がそのまま読まれる。
> レビュアー自身が重大度と基準の記号を付ける以上、判定規則はレビュアーが読む文書に無ければならない。

### 9.1 報告してよいもの（許可列挙。列挙外は報告しない）

| 群 | 軸 | 実体 |
|---|---|---|
| **A 内的要因** | 矛盾が無い（**起点は、触った機能 ID の節とその参照先。機能 ID が無い PR では、差分の触ったファイルと、そこに書かれた規則の名前を起点とする**。そこから下流をたどって適用先の全体を見る——9.3。全文書を端から読むことはしない） | 4章「ドキュメント」 |
| | 実装可能である（書けないものを「書け」と指示していないか） | — |
| | 参照実装を写しても穴が無い | 4章「コメント」 |
| | 検査・テストが偽の緑を出さない（**視点は1つ**。6章） | 6章「有効性」 |
| | データ整合が DB 側で担保されている（アプリの検証1箇所の漏れで壊れないか） | [CLAUDE.md](CLAUDE.md) 2 |
| | エラー時に失敗が名指しされる（握り潰していないか） | 3章「エラーメッセージ」 |
| | 依存・版の整合（[技術スタック](docs/tech-stack.md) と実際） | — |
| | 環境差（手元・CI・デプロイ先でずれが緑のまま隠れないか） | — |
| | データの流れと責任の所在（**認可の判定がサーバーにあるか**） | 2.2 |
| **B 外的要因** | セキュリティ12観点 | 3章 |
| | 認可の4経路 ＋ オーナーの閲覧範囲 | 2.1 |
| | 秘密の露出（レスポンス・ログ・コミット） | 3章 / [CLAUDE.md](CLAUDE.md) 禁止事項 |
| | 法令（電気通信事業法・個人情報保護法・外部送信規律・**商標とトレードドレス**・絵文字ライセンス） | [要件定義書](docs/requirements.md) 6 |
| | **顧客要望**（このプロジェクトでは提出要件＝課題文） | 2.3 |
| | 要件の3分類（承認の有無・区分の記載） | 2.3 |
| **C 品質属性** | 性能 | 5章 |
| | 可観測性 | [要件定義書](docs/requirements.md) 4.6 |
| | 保守性（**4.7 が挙げる具体項目だけ**: イベント型の一元化・REST 仕様からの型生成・生成物の差分検証。一般語としての「保守性」は見ない） | [要件定義書](docs/requirements.md) 4.7 |
| | 可用性（**バックアップ・復旧手順・アラートが書かれているか**。フェイルオーバーは「有無が決まっていて、無いなら代償が書かれているか」だけを見る。**どちらを採るかは指定しない**） | [要件定義書](docs/requirements.md) 4.2 |
| **D 文書の性質** | 決定が、実装者が読む場所に届いているか（`schema.prisma` の docblock・参照実装） | — |
| | 踏むと壊れる条件の注記が**無い**こと | 4章 |
| **E プロセス** | 責任の所在（仕分け・打ち切り・マージの条件は依頼側。**レビューは申告のみ**） | 0章 |
| | PR の目的・機能 ID と差分が一致し、**目的外の変更を含まない** | 8章 |

### 9.2 🔴 と 🟡 の閾値

**🔴 は「直さないと実装が壊れる／外的要因に触れる」。型ごとの具体は次のとおり。**

**判定は動線で行う。** この文書群は、上流から下流へ流れる。

```
課題文（顧客要望）
  → 要件定義書（決定・非機能）
    → 機能一覧（受け入れ条件・区分）
      → schema.prisma の docblock / 参照実装（実装者が最初に読み、写す）
        → 実装
CLAUDE.md・REVIEW.md（規則） → 作業のやり方
scripts のコメント → 次に触る人の変更
```

**🔴 になるのは、次の2つを同時に満たす食い違いである。**

> **(a) 上流の成果物に書かれており、かつ**
> **(b) その食い違いが、下流の選択を変える。**

**(a) だけでは足りない。** この repo はほぼ全部が上流であり、
「上流の食い違いは 🔴」とすると文書の食い違いがすべて 🔴 になって、範囲が元に戻る。

**(b) は次の1問で判定する。**

> **その記述に、そのまま従った結果が壊れているか。**

**従う対象があるもの**（指示・雛形・手順・参照実装）が実態と違えば 🔴。
**従う対象がないもの**（説明・診断の文言・経緯）のずれは 🟡 である。
「実装に関わる記述かどうか」で切らない。**それでは文書のほとんどが 🔴 になり、範囲が元に戻る。**

- 「件数は CI が引き受ける」と書いてあるが、CI が照合するのは固定書式の宣言だけ →
  従うと**照合されない件数を書いて通す** → 🔴
- NG のメッセージが誤った原因を名指ししている → **従う対象が無い**（読む人は原因を探す。検査は現に落ちる）→ 🟡
- 同じ代償が2つの節に全文で書かれている → **下流はどちらを読んでも同じものを作る** → 🟡

**この形は、適用先を探すのにも使う。** 上流で1箇所見つけたら、**下流をたどって**
その食い違いが届く先をすべて挙げる（9.3「整合性の単位」）。

| 型 | 🔴 になるもの | 🟡 になるもの |
|---|---|---|
| 認可・秘密 | **すべて。🟣 も含む**（1章のとおり 🔴 と同じ扱いで、その PR で直す） | — |
| テスト | **必須8項目**（6章）が無い・効いていない | それ以外のテスト不足 |
| 動作 | **結果が変わる**（緑と赤が反転する・戻り値が変わる・環境によって止まる） | **原因の名指しが誤る**（赤にはなる。診断の質） |
| コメント | **行動を指示する**もの（「消してよい」「この形で書け」「足すな」）と**参照実装**の記述が、実態と違う | 記述と実態のずれ（指示形ではないもの） |
| 文書間 | **条件・キー・認可境界・値**が2文書で違う | 重複・表題・番号の不一致 |
| 踏むと壊れる注記 | 被参照側に注記が無く、**写すと壊れる**場合 | 診断の質に留まる場合 |

**判定例**

| 実例 | 判定 | 理由 |
|---|---|---|
| 参照実装の docblock が、要求者の識別子を「リクエスト由来」と書いている | **🔴** | 写した実装が、他人の識別子を受け取って認可を破る |
| `lower()` で照合するテストが、片側だけを固定していて改変で落ちない | **🔴** | 偽の緑。受け入れ条件が静かに落ちる |
| 失敗の名指しが誤っていて、原因から遠い案内が出る | 🟡 | 赤にはなる。誤るのは診断の指し先だけ |
| 同じ代償が2つの節に全文で書かれている | 🟡 | どちらも現時点では一致している。正典に当たれば戻れる |
| コメントが「N 行目」「直上」を指している | ⚪ | 報告しない（0章の「誰も見ない」） |

**「🔴 か 🟡 か」が割れるものは、レビュアーが自分の読みを述べたうえで「判定は依頼側」と明記する。**
**仕分けを確定するのは依頼側である**（E）。

### 9.3 整合性の単位と「壊れている」の判定単位

**整合性を取る単位は「同じ規則・決定・記述が当たる箇所の全体」。**
編集された近傍でも、全文書でもない。境界は規則が決める。

**「壊れている」の判定単位は「規則の適用状態」。**
1箇所でも欠けていれば、それは「その規則が未適用」であって「その1箇所が壊れている」ではない。
したがって**報告も修正も、適用先の全体を単位とする**（8章）。

**「1 PR 1目的」の「目的」には、「1つの規則・決定を、その適用先全体に当てること」を含む。**
複数のファイル・複数の機能に跨っても、規則が1つなら1目的である。
**適用先が PR の目的を超える場合は、規則単位の別の PR に分ける。**

### 9.4 巡回の形

1. **基準段階** — **🔴 と、認可・秘密に関わる 🟣 を直す。** それ以外の 🟣 と 🟡 は
   その巡回でイシューに記録する（テスト不足はテスト追加のイシューに集約）。
   **🔴 と、認可・秘密の 🟣 が出なくなったら**次へ。**巡回数の上限は置かない**（閾値で有限になる）
2. **仕上げ** — コメント・文章の修正を**1回だけ**入れる。「減るまで回す」はしない

**マージは巡回に含まれない。** 0章のとおり、可否を判断するのは人間である。
巡回が終わったら、**CI 4本（`code` / `docs` / `audit` / `review`）の完走を確認して報告する**
（`review` の `success` は「完走した」であって「指摘なし」ではない）。
**マージするかどうかは、開発フロー 0 で確認した権限の範囲による**（[CLAUDE.md](CLAUDE.md)）。

**打ち切りは、回数ではなく理由で決める。** 止めるのは次の2つのときだけである。

- **直す方法が無い**——規則そのものに反する、定義に反する、不変条件どうしが矛盾する（直せば別の何かが壊れる）
- **判断が人に属する**——設計判断が絡む（受け入れ条件・区分・認可境界・データモデル・非機能の数値が変わる）

**直す方法があるなら、何巡目でも直す。** 回数は判定の材料であって条件ではない。
ただし**同じ束が5巡以上になったら、要因を記録して依頼側へ報告する**（巡回は止めない。止めると実装が遅れる）。
回数が増えるのは、難しくなっているか、大きい原因が潜んでいるかの兆候である。

**「同じ束」は、規則の名前で判定する。** 台帳（PR 本文）の各束は規則の名前と不変条件を持つ。
新しい指摘が守らせようとしている規則が、台帳のどれかと一致すれば同じ束である。
**箇所が違っても、記述が新しくても、規則が同じなら同じ束として数える。**

| 見え方 | 実体 | 直し方 |
|---|---|---|
| 前巡と同じ箇所・同じ内容 | **直っていない** | 直し方が誤っている。設計を変える |
| 同じ規則が**別の箇所**で出た | **適用先の列挙が漏れた** | `grep` をやり直し、残りを一度に直す |
| **前巡で足した記述**が対象になった | **自己生成** | 足した記述を**消す方向**で直す |

**3つとも同じ束として数える。** 「新規かどうか」で数えると、
**適用先の漏れと自己生成が「新しい指摘」に見えて、5巡以上での要因の報告が発動しない**
（実測: 1つの規則の適用漏れが5巡、足した記述への指摘が2巡続いた）。

**台帳に、束ごとの出た巡回を記録する。** 同じ束が続くことは、上の2つの理由の兆候である。
数えるのは指摘の件数ではなく、**束が出た巡回の数**である。

**より良い方法に気づいたときは、それを理由に止めない。** できる範囲で直し、**手戻りできる形**にしたうえで
依頼側へ報告する（[CLAUDE.md](CLAUDE.md)「手戻りできる形にする」）。**採るかどうかは人が決める。**
これは提案であって作業報告ではない。**置き場は環境で決まる**——一人で開発しているならイシューにしてよく、
複数人ならローカルに一時的に置いて人の確認を得てからイシューにする（[CLAUDE.md](CLAUDE.md)「手戻りできる形にする」）。

**別の束の 🔴 は、何巡目であってもそのまま直す**（打ち切りの対象ではない）。

**イシューに落とすもの**: 🟡 のすべて（テスト不足はテスト追加のイシューへ集約）、
上限を超えて見出しだけで返った分のうち 🟡 のもの。
**🔴 と、認可・秘密に関わる 🟣 は落とさない**（直すか、規則単位の別 PR に分ける）。

**機能 ID の行が無い PR の既定**: 差分から推定して報告の冒頭に「（推定）」と明記し、
E の 🟡 として1件報告する。

**「機能 ID: なし」と書かれている PR**: 推定も 🟡 の報告も**しない**。宣言されている以上、
書かれていない場合には当たらない。**B・C のうち「機能要件」だけを見ない**（受け入れ条件を満たすか、区分が書かれているか、
性能の目標値を外していないか等）。**秘密の露出・法令・認可の4経路は、どの PR でも見る**——機能要件ではなく、
1章の「🟣 でも認可・秘密に関わるものは 🔴 と同じ扱い」が無条件であることとも対応する。

**記録**: 巡回数をこのファイルに書かない。**実数は、そのブランチのレビューのワークフロー実行履歴から数える。**

---

## 出典

本ファイルの観点は、以下の公開されている実務基準に基づく。

| 基準 | 発行元 | 本ファイルでの利用 |
|---|---|---|
| [Code Review Developer Guide — What to look for in a code review](https://google.github.io/eng-practices/review/reviewer/looking-for.html) | Google | 4章（設計・機能・一貫性・コメント・踏むと壊れる条件・ドキュメント）。**複雑さと命名は、このプロジェクトでは 0章の「誰も見ない」に置いた**（Google は Nit として扱うが、記憶を持たないレビュアーでは毎回新しく見つかるため外した）。「将来の問題への備えを推奨しない」という方針はここに拠る |
| [CWE Top 25 Most Dangerous Software Weaknesses](https://cwe.mitre.org/top25/) | MITRE | 3章の優先順位。2025年版では **XSS が1位、SQL インジェクションが2位、CSRF が3位**（39,080件の CVE を集計） |
| [OWASP Application Security Verification Standard (ASVS) 5.0](https://owasp.org/www-project-application-security-verification-standard/) | OWASP | 3章のセキュリティ観点。5.0（2025年5月）は17章・約350要件で構成される |
| [安全なウェブサイトの作り方 改訂第7版](https://www.ipa.go.jp/security/vuln/websecurity/index.html) | IPA（情報処理推進機構） | 3章。国内の実務で最も広く参照される。**「保険的対策は根本原因を除去しないため、それのみに依存する設計は推奨しない」**という指針を、本プロジェクトの XSS 対策（サニタイズではなく HTML を生成しない構造）に反映している |

### 出典を選んだ理由

- **Google の指針**は、コードレビューの実務基準として最も広く引用されており、
  「何を見るか」と「何を見ないか」の両方を明示している点で、AI への指示に適している
- **CWE Top 25** は実際の CVE を集計した実績データであり、**推測ではなく発生頻度に基づく優先順位**を与える
- **OWASP ASVS** は検証可能な要件の形をとっており、「安全にする」という抽象論ではなく
  チェック可能な項目に落ちている
- **IPA の資料**は国内の開発現場で共通言語として使われており、日本語で参照できる
